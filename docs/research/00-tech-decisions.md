# 00 — Shed Book: Canonical Tech Decision Record

**Status:** FINAL. This file supersedes the ten research notes in `docs/research/raw/` wherever they disagree with it.
**Authority order used to produce it:** `shed-book-spec.md` → the four critiques in `docs/research/critique/` → the ten research notes.
**Rule applied:** where a critic corrected a researcher, the correction wins. Where a critic could not verify their own correction, the item is marked **UNPROVEN** and carries a mandatory empirical check.
**Date:** 2026-07-27. **Owner:** technical lead.

> A developer who reads only this file knows what to build with. The ten research notes are the *evidence*; this file is the *decision*. If a note contradicts a row in §2, the note is wrong and must be edited or quarantined before the engineering doc set is written.

---

## How this document is organised

| § | Contents |
|---|---|
| 1 | The five decisions that must be taken before commit #1 |
| 2 | The canonical decision table — 129 numbered, final, implementable decisions |
| 3 | The offline-purity contract: the promise, the mechanical gates, the honest wording |
| 4 | Dropped, degraded and opt-in-gated — the blunt list |
| 5 | The verified dependency table (from the c1 audit; the only source of version numbers) |
| 6 | Corrections applied — what the critics overturned, so it is not re-litigated |
| 7 | Open questions only the owner can answer |
| 8 | The engineering doc set to write |

---

## 1. The five decisions that must be taken before commit #1

These are irreversible or structural. Everything else in this document can be revisited in week two without rework.

1. **`flutter_riverpod: 2.6.1`, pinned exactly.** Riverpod 3.x cannot resolve alongside `drift_dev`. Every Riverpod-3-only API is banned from the codebase and from the docs. (c3 A1, c2 §1–3, c1 BLOCKER-3)
2. **Instants are `INTEGER` UTC epoch millis; civil dates are `TEXT 'YYYY-MM-DD'`.** `store_date_time_values_as_text` is never set and drift `dateTime()` columns are never used. This is irreversible after the first migration snapshot. (c3 A4)
3. **Withdrawal clear date = ceil-to-next-local-midnight of (administration instant + N × 24 h)**, computed in absolute time, stored exactly once at write time. Civil-day arithmetic is banned for withdrawal. (c3 A2, A3)
4. **`main()` awaits nothing**: `ensureInitialized()` → install error handlers → `runApp()`. The database lives in **application support**, opened after the first frame. (c3 A5, D1)
5. **Run the manifest-merger check against a real release AAB before writing any `tools:node="remove"` line**, and add the four `in_app_purchase*` packages to the offline allowlist and `com.android.vending.BILLING` to the permission list. (c3 B1, B2) — **done 2026-08-01, N02-T01.** The record is §3.3; the ruling it produced is [`13-build-ci-release.md`](../engineering/13-build-ci-release.md) §2.2.

~~Until #5 is done, the offline gate in CI is unwritten, not merely unimplemented.~~ **Struck
2026-08-01: #5 is done.** G1 is now writable, and N31-T03 is what writes it. Nothing else about the
sentence changed — the gate is still unwritten *code*, and no CI job builds an `.aab` until N31.

---

## 2. The canonical decision table

Every row is final and implementable. "Source" names the primary evidence; the bracketed reference names the critique or note that settled it.

### A. Toolchain and dependency resolution

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 1 | Flutter/Dart pin | **Flutter 3.44.8 stable (2026-07-23), Dart 3.12.2**, pinned via FVM. Never `channel: stable` unpinned. Any bump re-runs the full resolution matrix. | c1 verified 3.44.8 is current stable and that its `meta: 1.18.0` / `test_api: 0.7.11` / `intl: 0.20.2` pins are identical to 3.44.6's, so no resolution conclusion flips. [c1 MEDIUM-7] | Floating `stable`; staying on 3.44.6 without a stated pin |
| 2 | Analyzer ceiling | **No Flutter app can resolve `analyzer ≥ 13.1.0`**, because the SDK pins `meta: 1.18.0` exactly. This single fact governs the whole dev-dependency set. | Read from `packages/flutter/pubspec.yaml` on stable. [c1 §1] | Assuming latest-is-fine for any analyzer-coupled package |
| 3 | `build_runner` constraint | **`build_runner: ">=2.15.0 <2.15.2"`** | 2.15.2 requires `analyzer >=13.3.0` → `meta ^1.18.3` → unresolvable. [c1 BLOCKER-1] | `^2.15.2` (four notes recommend it; it does not resolve) |
| 4 | `package:test` as a direct dep | **Never declared.** A Flutter app uses `flutter_test`, which does **not** depend on `package:test`. | `test` caps `analyzer <13.0.0`, which breaks `drift_dev ≥2.34.1`. `test 1.31.2` pins `test_api 0.7.13` and cannot coexist with `flutter_test` at all. [c1 BLOCKER-2, HIGH-5] | `test: any` in dev_dependencies (note 03's shipped pubspec) |
| 5 | Evidence of resolvability | **Before any other work:** run `flutter pub get` against §5's table on Flutter 3.44.8 and **commit the resulting `pubspec.lock`** as the doc set's evidence. CI re-asserts it. | c1 action item 10; note 02's resolution experiment was run against a pubspec that included the EOL `sqlite3_flutter_libs`, so its "117 packages" figure is not the project's. [c1, c2 §8, c3 D10] | Publishing a pubspec nobody has resolved |

### B. Architecture, layering and error handling

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 6 | Overall shape | Flutter's **MVVM two-layer** (UI + Data) with the domain layer as **plain top-level functions**, not use-case classes. | Flutter's own guide pre-authorises skipping the logic layer for CRUD apps; the domain here is stateless arithmetic. [note 01 §1.1, §5] | Clean Architecture; use-case/interactor classes; separate API vs domain models |
| 7 | Flutter's `offline-first` design pattern | **Do not follow it.** It is a cache-over-network pattern. Every mechanism it prescribes is dead weight or a banned network path. Keep note 01 §1.4 verbatim in the engineering doc as the anti-regression artefact. | `docs.flutter.dev/app-architecture/design-patterns/offline-first` composes an `ApiClientService` with a `DatabaseService`. [note 01 §1.4; c3 B3 #1] | `connectivity_plus`, `workmanager`, `battery_plus`, FCM, `Timer.periodic` sync, a `synchronized` flag |
| 8 | Folder layout | **Feature-first UI, shared `data/` + `domain/`** — the Compass App hybrid. Single package. 9 feature folders for 12 screens. | "The `data` folder organizes code by type… the `ui` folder organizes by feature" — Flutter case study. Export must read all 8 entities; pure feature-first forces 8 sibling imports. [note 01 §3] | Pure feature-first; layer-first; `lib/src/`; a separate pure-Dart domain package; pub workspaces; melos |
| 9 | Dependency direction | **8 layer rules**, enforced by a dependency-free Dart script. `lib/features/**` may never import `core/db/`, `package:drift/`, `package:sqlite3/`, or a sibling feature. `lib/data/` may never import `package:flutter/material.dart`. | A package boundary expresses 1 of the 8 rules; a 60-line script expresses all 8 and cannot rot on an SDK upgrade. [note 01 §4.3, §11] | `import_lint` (analyzer ^12.1.0 conflicts with `drift_dev` ^13.0.0 — unresolvable), `custom_lint` (upstream archived 2026-03-24), `dart_code_metrics` (discontinued) [c1 HIGH-4] |
| 10 | Number of source-scanning gates | **One** `tool/check_policy.dart` with a rule table, one allowlist file, one exit code. It subsumes layer rules, banned text, design tokens and the dependency allowlist. | Five bespoke gates each with its own false-positive surface, before a single screen exists, is ceremony a solo developer will delete. [c3 §E] | Separate `check_layers` + `check_offline` + `check_tokens` + a policy-test suite |
| 11 | Write path | Repository methods are **event verbs**, never `save(aggregate)`. Draft state is **unrepresentable** — there is no aggregate parameter to defer. The row is created on screen entry, not on exit. | Spec §5 "every write is committed immediately… there is no draft state to lose" is only structural if no aggregate write method exists. [note 01 §6.2, §8] | A `Save` button; `LambingDraft`; `isDirty`; `commit()` |
| 12 | Read path | **drift `watch()` streams, one SQL statement per screen.** Never `combineLatest` two drift streams. `.distinct()` in the repository; `override_hash_and_equals_in_result_sets: true`. | drift#3338 (open): two streams updated in one transaction can emit at different times — maintainer: *"generally is working as intended"*. drift#3295 (open): streams re-run on any write to a tracked table. [note 01 §6.3] | `combineLatest`/`Rx.combineLatest*` over drift streams; manual `ref.invalidate` after writes |
| 13 | Error handling | **Reads throw; writes return a sealed `WriteOutcome`** (`WriteCommitted{flags}` / `WriteFailed{ShedFailure}`). `Result<T>` on every method is disproportionate. Failure case is never named `Error` (shadows `dart:core`). | The third state on writes is *rejected on domain grounds* (spec §12.4) — neither an exception nor a bool can carry it. ~100% of reads succeed and every failure has the same response. [note 01 §9] | `Result<T>` everywhere; `result_dart`; naming the failure `Error` |
| 14 | Global error net | `FlutterError.onError` + `PlatformDispatcher.instance.onError` + a **dark** `ErrorWidget.builder`, all writing to a local capped ring-buffer file. No `runZonedGuarded`. No `exit(1)` in release. | Flutter's own errors page sets all three and uses no `runZonedGuarded`; `PlatformDispatcher.onError` supersedes it and zone/binding mismatch is a documented footgun (flutter#94123). The default red-on-yellow `ErrorWidget` is a flashbang under a head torch. [note 01 §9.3, note 08 §7.3] | Crashlytics/Sentry/Bugsnag (network path + exfiltration of spec §4.5 data); `runZonedGuarded`; killing the app on a framework error |
| 15 | Abstract repository interfaces | **No.** Concrete `final class` repositories. | Real in-memory SQLite is a better fake than a hand-written fake and cannot diverge from production. Contradicts Flutter's "strongly recommend", deliberately. [note 01 §14.4] | `abstract class LambingRepository` with one implementation forever |
| 16 | Codegen | **drift only.** One generator is the budget. | build_runner's incremental model is O(N²)-ish (dart-lang/build#3555). `freezed` 3.2.5 is unresolvable on this stack anyway (`analyzer >=9.0.0 <11.0.0` conflicts with both `drift_dev` and `build_runner`). [c1 LOW-13, note 01 §15] | `freezed`, `riverpod_generator`, `json_serializable` for entities, `mockito` |

### C. State, DI, navigation, bootstrap

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 17 | State management + DI | **`flutter_riverpod: 2.6.1`, exact pin** (not `^2.6.1`). Providers written by hand. `ProviderScope(overrides: […])` in `main()` is the whole DI root. | Riverpod 3.4.1 declares `test: ^1.0.0` at runtime (WONTFIX, riverpod#4791); on this app's stack `flutter pub get` **fails outright** — reproduced by two independent reviewers. [c3 A1, c2 §1, c1 §4.3] | Riverpod 3.x; `hooks_riverpod`; `riverpod_generator` 4.0.6 and `riverpod_lint` 3.1.6 (both **internally unresolvable**); `get_it`; BLoC; `signals` |
| 18 | Banned Riverpod-3 APIs | `ProviderScope.retry`, `ProviderContainer.test()`, `WidgetTester.container`, bare `Notifier` + `.autoDispose`, Mutations, `AsyncValue.valueOrNull`, `StateProvider`, `StateNotifierProvider`, constructor-delivered family args. **CI greps for each.** | Verified compile errors on 2.6.1: `'PenWriteController' doesn't conform to the bound 'AutoDisposeNotifier<…>'`, `The named parameter 'retry' isn't defined`. On 2.6.1 there is no auto-retry to disable, so note 01's "non-negotiable" `retry:` line is both uncompilable **and** unnecessary. [c2 §2, §3; c3 A1] | Porting note 01's pitfall #4 and its CI grep for `retry:` |
| 19 | Riverpod 2.6.1 spellings | Families: `AutoDisposeFamilyAsyncNotifier<T, Arg>` with `build(Arg arg)` / `this.arg` and a **zero-argument** constructor tear-off. Auto-disposing notifiers: `AutoDisposeNotifier`. Containers in tests: `ProviderContainer(...)` + `addTearDown(container.dispose)`. | `pub.dev/documentation/riverpod/2.6.1/riverpod/FamilyAsyncNotifier-class.html`. Constructor delivery is a Riverpod **3** change. [c2 §3] | Note 02 §4.4's snippet, which is Riverpod-3 code labelled "2.6.1 spelling" |
| 20 | Provider shapes | `databaseProvider` is a **`FutureProvider<AppDatabase>`** opened on the first post-frame callback. Repositories are `Provider`s that `ref.watch` it. No `Provider<AppDatabase>` overridden with a value in `main()`. | The DB open path is inherently async once `databaseDirectory: getApplicationSupportDirectory` lands, so "construct eagerly, nothing is awaited" is a distinction without a difference. [c3 A5, c4 §1] | Note 01's `Provider<AppDatabase>.overrideWithValue(db)`; note 02's `await openShedBookDatabase()` |
| 21 | Bootstrap | `main()` = `WidgetsFlutterBinding.ensureInitialized()` → install sync error handlers → `runApp()`. **Nothing awaited.** DB open, migration, `path_provider`, timezone DB, settings/palette, notification init and the purchase-stream subscription all happen after the first frame. The first frame is a static dark Quick Entry shell with a **fully interactive keypad and no data**. | It is the only bootstrap that satisfies both "no white flash" and "no error-less black screen". `runApp()` calls `ensureInitialized()` internally anyway, so omitting the explicit call moves binding init by microseconds. Because every theme is dark, a wrong first frame is a dark first frame. [c3 A5, c4 §1] | Note 01's "await nothing but construct the DB eagerly"; note 02's four awaits; `deferFirstFrame`; `flutter_native_splash.preserve` |
| 22 | Double-tap protection | A `WriteController` base class whose `guard()` refuses to run concurrently (`if (state is WriteRunning) return;`). One `tester.tap(); tester.tap();` test per destructive action. | Cold, wet fingers on capacitive glass double-fire. Flutter's own Command pattern exists partly for this. This is a UX safety feature disguised as architecture. [note 01 §8.5] | Riverpod 3's experimental Mutations |
| 23 | Navigation | **`Navigator` 1.0 + a static typed route-helper file** (`Routes.eweCard(context, id)`), one method per screen. | go_router's value proposition is URLs; there is no web target, no deep links, no URL bar. Three majors in ~24 months, plus open restoration bugs flutter#117683 (since 2022-12-27) and #174935. [note 01 §7.3, c1 §4.3] | `go_router` 17.3.0; named routes; `ShellRoute` bottom nav |
| 24 | State restoration | **None.** No `RestorationMixin`, no `restorationScopeId`, no iOS storyboard restoration-ID step. Resume to Quick Entry with nothing selected after ~2 minutes backgrounded. | Restoring a stale selected ewe at 3am is a data-integrity bug, not a convenience. The database *is* the restored state. [c3 C3, note 08 §4] | Note 02's keypad-query and scroll-offset restoration |

### D. Persistence and schema conventions

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 25 | Persistence layer | **drift over SQLite**, with `package:sqlite3` bundling the engine via Dart build hooks. Never the OS SQLite. | The file must be a plain, boring SQLite database a vet or a kid can open in 2036 — spec §4.3 makes a proprietary format a contradiction of the positioning. Bundling guarantees `STRICT` (needs ≥3.37.0) and FTS5 on every device. [note 03 §1.1, §2.4] | `sqflite` (OS SQLite = a per-device feature lottery), `sqlite_async` (no schema snapshots/`SchemaVerifier`), `objectbox`/`isar`/`hive`/`hive_ce`/`realm`/`sembast` |
| 26 | `sqlite3_flutter_libs` | **Never a direct dependency.** It arrives transitively via `drift_flutter` 0.3.1 as a no-op EOL shim and that is expected and harmless. | pub.dev's `isDiscontinued` flag is **false** for `0.6.0+eol`; what exists is an `+eol` build tag and a caution box. A CI check keyed on the discontinued flag will not fire on it. [c1 MEDIUM-9] | Calling it "DISCONTINUED" in the docs; adding it to `pubspec.yaml`; implying the app can be free of it |
| 27 | Database location | **`getApplicationSupportDirectory`**, overriding `drift_flutter`'s Documents default. | If `UIFileSharingEnabled` is ever set so users can grab exports from Files, all of `Documents/` becomes user-visible and user-**deletable**. A shepherd tidying up and deleting `shed_book.sqlite` is a product-ending bug. Both directories are equally backed up. [c3 D1, note 03 §3.1] | `driftDatabase(name: 'shed_book')` with the default directory |
| 28 | Connection pragmas | `journal_mode = WAL`, **`synchronous = FULL`**, `foreign_keys = ON`, `busy_timeout = 5000`, set in a top-level `setup` function (it crosses an isolate boundary and must capture nothing). | The universal mobile advice is WAL + `NORMAL`; sqlite.org states plainly that NORMAL in WAL "does lose durability… might roll back following a power loss". Spec §5 says "assume the phone dies". One extra fsync per commit at ~10 writes/minute is free. [note 03 §7.2] | `synchronous = NORMAL`; `readPool` / multi-isolate drift |
| 29 | DateTime storage | **Instants → `INTEGER` UTC epoch millis behind `extension type Instant(int)`. Civil dates → `TEXT 'YYYY-MM-DD'` behind `extension type LocalDate`.** `store_date_time_values_as_text` is **not set**; drift `dateTime()` columns are not used anywhere. | An instant and a civil date are different kinds: a lambing *happened at a moment*, a withdrawal *clears on a date*. drift's single global build flag forces one representation onto both. This also composes with decision #38. **Irreversible after the first migration snapshot.** [c3 A4] | Note 03's `store_date_time_values_as_text: true`; drift's default integer-seconds mode (*"drift always returns a non-UTC value"*) |
| 30 | Cost of #29, stated | Note 03's `mixin Identified` (`createdAt`/`updatedAt` as `dateTime()`), its `CHECK (end_date IS NULL OR end_date >= start_date)` constraints, and every `dateTime()` column in its schema **must be rewritten** as `integer()` or `text()` before the first snapshot is generated. | Cheap on day 0, impossible on day 400. [c3 A4] | Quietly carrying note 03's schema forward |
| 31 | Table conventions | Every table `STRICT`. Every relationship a real FK with an explicit `ON DELETE`. Every FK hand-indexed. `PRAGMA foreign_keys = ON` on every connection. History tables, not mutable "current" fields. No column that could encode veterinary advice has a `DEFAULT`. **Amended 2026-08-01 (`CONVENTIONS` R79): the strike is a persistence convention, not a design-system aspiration.** A second mixin, `Struckable`, carries `struck BOOLEAN NOT NULL DEFAULT 0` and `struck_at INTEGER NULL` on the **twelve** record-bearing tables, with `CHECK (struck IN (0,1))` and `CHECK ((struck = 1) = (struck_at IS NOT NULL))`. The default on `struck` is not a defaulted advice column — the ban is on `days`, `ease` and `status`. Struck rows are excluded from every count and included in every history and every export. | SQLite creates no child-key index automatically and FK enforcement is off by default (sqlite.org/foreignkeys.html). `STRICT` makes the DB refuse garbage rather than store it and pretend — spec §12.4 at the storage layer. [note 03 §4, note 09 #18] | Dynamic typing; `noAction` by laziness; a mutable `rearing_dam` or `occupant_ewe` column |
| 32 | ID strategy | **Dual key**: `INTEGER PRIMARY KEY AUTOINCREMENT` for joins/FKs + `uid TEXT UNIQUE` holding a **UUID v7** as the export/re-import identity. Import is an upsert on `uid`, never on `tag`. | v7's 48-bit ms prefix keeps the uid index appending. A `TEXT PRIMARY KEY` costs an extra index lookup on every join and breaks FTS5 external-content's `content_rowid`. `AUTOINCREMENT` stops a recreated ewe inheriting a culled ewe's notes via a stale FK in an old export. [note 03 §5] | UUID-only PKs; integer-only; `ulid` (community spec, 22 months stale, less legible in a CSV) |
| 33 | Fostering | Immutable `birth_dam` on `Lamb` (enforced by a SQL `BEFORE UPDATE` trigger) + append-only `FosterEvents` + a SQL **view** for the current rearing dam. `birthDamId` is `final` with no `copyWith` that accepts it. | Sheep genetic evaluation models birth type and rearing type as distinct traits (`TBR`). A denormalised `rearing_dam` column is a dual write a future code path will get wrong. [note 03 §4.6, note 09 Part 5] | A mutable `rearing_dam` column |
| 34 | Pen occupancy | Append-only `PenOccupancies` + a **partial unique index** `WHERE exited_at IS NULL`. "Hours since penned" = `now − entered_at` on the open row. | The database physically refuses two ewes in pen 3 at once. This is "the whiteboard gets wiped" solved at the storage layer. [note 03 §4.7] | A mutable `occupant_ewe` column on `Pens` |
| 35 | Search | **Two different problems.** Partial tag matching (`12` → 412) is an **in-memory ranked filter over ~400 cached tags in Dart**, ranked exact > prefix > suffix > infix then most-recently-touched. Full-text note search is **FTS5** over a single `search_docs` fan-in table kept in sync by SQL triggers, 200 ms debounce, on its own screen. | sqlite.org/fts5.html: trigram *"substrings consisting of fewer than 3 unicode characters do not match any rows"* — the spec's own `12` example is FTS5's counter-example. `LIKE '%12%'` cannot use an index. At 400 ewes a Dart filter is sub-millisecond and updates in the **same frame**; a SQL round-trip through drift's background isolate lands one or two frames late. [note 03 §6.1, note 08 #7] | FTS5 or trigram for tags; debounce-plus-query on the keypad path |
| 36 | FTS5 availability | **A startup assertion, not a runtime capability probe.** `package:sqlite3`'s bundled build documents `SQLITE_ENABLE_FTS5`; assert it once at open and fail loudly. | The probe is unnecessary under note 03's evidence; a permanent runtime branch is dead weight. [c3 D10] | Note 09's runtime capability probe with a `LIKE` fallback |
| 37 | Migrations | **Forward-only, additive-by-default, never destructive.** `dart run drift_dev make-migrations`; committed schema snapshots; `stepByStep`; `SchemaVerifier.migrateAndValidate` for **every from→to pair**; `PRAGMA foreign_key_check` returns zero rows after every path. A red migration test is ship-blocking. | The only backup is a user-initiated export. A migration bug destroys five seasons with no recovery path. Never `DROP COLUMN`/`DROP TABLE` on user data; never change a column's meaning in place. [note 03 §9, note 04 §3.5] | N-1→N testing only; hand-rolled `ALTER TABLE` rewrites |
| 38 | Migration test scope | Full from→to matrix with `migrateAndValidate` (non-negotiable) + a CI check that `make-migrations` produces **no git diff**. `testWithDataIntegrity` applies to the **N-1→N pair and any migration that rewrites a table**, not to every pair. | The from→to matrix is the high-value half; quadratic data-integrity busywork at v1 is not. [c3 §E] | `testWithDataIntegrity` on every pair |
| 39 | Debug schema self-check | In `beforeOpen`: `if (kDebugMode) { await db.validateDatabaseSchema(); }`, importing `package:drift_dev/api/migrations_native.dart`. | `validateDatabaseSchema` is an **extension member** on `GeneratedDatabase`, not a top-level function — note 03's `validateDatabaseSchema(this)` does not compile. Wrapping a `Future<void>` in a synchronous `assert` starts the check and returns `true` immediately, so a mismatch surfaces as an unhandled async error long after `beforeOpen` completed. [c2 §4] | Note 03's `assert(() { validateDatabaseSchema(this); return true; }())`; the deprecated `api/migrations.dart` |
| 40 | Media storage | **Filesystem, not BLOBs.** `<appSupport>/media/YYYY/MM/<uuidv7>.<ext>`. **Store relative paths only**, resolved against a freshly obtained root every run, with `CHECK (relative_path NOT LIKE '/%')`. Caps: photo longest edge 2048 px JPEG q80; voice note AAC-LC mono `.m4a`. Missing files set `missing_since`; the row is never deleted. | The iOS container UUID is not stable across launches (flutter#23957); absolute paths 404 after every restore, update and re-install — and never reproduce on the developer's Android phone. `VACUUM INTO` copies the whole DB, and Android Auto Backup caps at 25 MB per app. [note 03 §10] | BLOBs in SQLite; absolute paths; deleting rows for missing files; opus (container differs per platform → broken cross-platform restore) |
| 41 | Encryption at rest | **None.** Rely on platform full-disk encryption. | Any key is a way the app fails to open at 3am, and `flutter_secure_storage`'s own page documents the Android-backup `InvalidKeyException` that makes a restore permanently unreadable. [note 03 §12, c1 §4.3] | SQLCipher; `flutter_secure_storage` |
| 42 | First-run bootstrap | **In the migration's `onCreate`, not in UI code:** insert one `Season` derived from the device clock (`year = now.year`, `label = "<year> lambing"`, `startDate = today`) and set `AppSettings.currentSeason` to it in the same transaction. Pens are created **lazily and implicitly** — the first time a ewe is penned the app offers pens `1…n` and creates the row on tap. The zero-pen board shows a single 72 pt "Add a pen" tile. | Every event table has `season NOT NULL REFERENCES seasons(id)` and `currentSeason` is nullable, so without this the first keypad tap cannot insert a lambing. Spec §5 forbids an onboarding wizard and §7.1 forbids blocking an entry to make the user set something up first. [c4 §2] | Asking the user to create a season; a first-run wizard; a nullable season on events |
| 43 | Care checkboxes (spec §7.2) | A **`CareEvents`** table keyed to `lamb` (nullable) and `lambing` (nullable) with the `CHECK ((a IS NOT NULL) + (b IS NOT NULL) = 1)` idiom: `kind IN ('colostrum','navel_dip','stomach_tube','warmed')`, the §12.5 provenance triple, `volume_ml`, `method IN ('teat','tube','bottle')`, `note`. Checkbox state is `EXISTS(...)`, not a boolean column. Completing the colostrum reminder writes the `CareEvent` — it is the same tap. | Spec §7.2 requires them and no table in the corpus stores them; the colostrum reminder currently has nothing to be marked complete *from*. `EXISTS` keeps "colostrum given at 03:22" recoverable. [c4 §4] | Boolean columns on `Lambings` |
| 44 | Ewe traits (spec §7.7) | An **`EweObservations(ewe, season, kind, occurredAt, note)`** table with an editable vocabulary seeded from the §11 authored list: `prolapse`, `mastitis`, `poor_mothering`, `good_mothering`, `no_milk`, `other`. Feeds the precomputed `ewe_summary` row. The table's doc comment states the §12.2 boundary: the app records *what the shepherd observed* and never infers "poor mothering" from a lamb death. | "3 seasons · avg 2.0 · assisted twice · prolapsed 2025" is not computable from free text, and §7.7's "filter the flock by anything" cannot include a free-text field. The recall feature is the product's whole reason to exist. [c4 §5] | `Ewes.notes` free text as the only home |
| 45 | Tag uniqueness | **Blocked on an owner decision (#7 in §7).** Either tags are unique forever, or unique within a status scope. Whichever is chosen, the "412 — culled 2025 — use anyway?" disambiguation prompt **moves off the create-on-the-fly path**. | Spec §7.1 and the 3am bottom-sheet rules both push against a dialog on the entry path. Must be settled before the schema is frozen. [c3 D10, F] | A disambiguation dialog at 03:20 |

### E. Domain correctness: time, units, statistics, the five safety rules

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 46 | Clock | **One clock: `package:clock`.** `clock.now()` everywhere. `DateTime.now(` appears in **exactly one allowlisted file**, `lib/core/time/app_clock.dart`. The scan skips `*.g.dart`. Store instants as UTC; convert at the presentation edge only. | Two clock seams is worse than none, because a test that fakes one does not fake the other. [c3 C1] | Note 02's bespoke `abstract class Clock` / `SystemClock` / `clockProvider` — **delete it**; `DateTime.now()` in five notes' example code (all must be rewritten before publication) [c2 §5] |
| 47 | SQL-side time | **Banned:** `CURRENT_TIMESTAMP`, `CURRENT_DATE`, `CURRENT_TIME`, `date('now')`, `datetime('now')`. All time arithmetic happens in Dart; SQL only compares and orders opaque integers and lexicographically sortable date strings. | Under decision #29 this mostly resolves itself; state it as the resolution rather than leaving it to collide. The bare tokens `strftime`/`datetime` are **not** banned — they false-positive and get weakened. [c3 C2] | Note 04's ban on bare `strftime`/`datetime`; `sqlite3_test` (only needed if SQL-side time is used, which is banned) |
| 48 | Timezone strategy | **`package:timezone` is confined to the notification-scheduling seam** (`tz.TZDateTime.from(dueAtUtc, tz.local)`). Everything else uses `Instant` + `toLocal()`. Keep note 04's DST cases and the `TZ=Pacific/Chatham` hostile CI job, asserting against the **device zone**, not an injected `tz.Location`. | A one-time-purchase app that a shepherd does not update for three seasons will have a *more* correct zone from the OS than from a bundled IANA snapshot frozen at build time. [c3 C9, note 09 #7] | `tz.Location` as an injected value used throughout the domain |
| 49 | Withdrawal clear date | **`clearDate = ceilToNextLocalMidnight(administrationInstant + N × 24 h)`**, absolute time, never civil days. The `elapsedCivil.inHours == 167` DST regression test is mandatory. Civil-day `DateTime(y, m, d + n)` arithmetic is narrowed to season boundaries and display-only date offsets. | VICH: the withdrawal period is the minimum period between last administration and production of foodstuffs, whole days rounded **up**. Measured: civil-day `+7` across the UK spring-forward yields **167 h, not 168** — one hour short, in late March, which is exactly when UK/Ireland lambing happens. [c3 A2, note 09 #3] | Note 01's `clearDateFor` and note 04's blanket "use calendar arithmetic for date offsets" |
| 50 | Is `clear_date` stored? | **Yes — it is the one stored derived value.** Computed exactly once at write time by the single `clearDateFor()` function; its inputs (`administered_at`, `withdrawal_days`) are stored alongside it forever; a mismatch raises `ClearDateDisagrees`, which is **shown, never applied**. | It is not really derived: it is *a record of what the app told the user* and printed into the medicine-book PDF handed to a vet or an abattoir. Spec §10 lists it on `Treatment`. The staleness objection is answered by the consistency flag, which is a §12.4-compliant answer rather than a silent correction. [c3 A3] | Note 09's "recompute, do not store"; a SQLite `STORED` generated column (cannot be added by `ALTER TABLE`; duplicates a safety-critical rule into SQL) |
| 51 | Withdrawal type (spec §12.1) | A hand-written **`sealed class WithdrawalPeriod`** with three states — `WithdrawalDays(n, target)` / `WithdrawalNotApplicable` / `WithdrawalNotRecorded` — with a private generative constructor and one entry point `WithdrawalDays.asEnteredByUser(days:, target:)`. Persisted as a **`treatment_withdrawals(treatment_id, target, days)` child table**: no default on `days`, **no row implies `NotRecorded`**, an explicit `NotApplicable` marker row. `WithdrawalTarget { meat, milk }` — 0..n entries per treatment. **Ruled 2026-08-01 (§7.0 row 10): `WithdrawalTarget` keeps both `meat` and `milk` in the v1 schema even though the v1 UI writes only `meat`** — the child table's `CHECK (target IN ('meat','milk'))` and its `{treatment, target}` unique key already express a second target at no cost, and re-admitting it after the freeze is a migration plus a re-read of every withdrawal surface. `WithdrawalMilkings` does not exist in v1. | A nullable `int?` conflates "the label says 0 days" with "I didn't look"; `0` is a real label value. One product routinely prints different meat and milk figures. A non-nullable single `int` column cannot express "I did not look at the bottle" — the exact confusion §12.1 exists to prevent. An `extension type` cannot give you a *private* generative constructor. [c3 D4, note 09 #1–2] | Note 04's `extension type` + single non-nullable column; note 03's `withdrawalDays = integer()()` on `Treatments`; any medicines lookup table |
| 52 | Proving "never default a withdrawal" | **Two gates only:** the schema assertion (the committed drift schema JSON has null `defaultValue` and null `clientDefault` for `days`) and the widget test. | Under #51 the sealed type makes the wrong state unconstructible. The source heuristic banning numeric literals near "withdrawal" fires on `CHECK` constraints and test fixtures. [c3 §E] | Note 04's four independent tests, including the numeric-literal heuristic |
| 53 | Timestamp provenance (§12.5) | A **`RecordedTime` value type**: `effective` + `capturedAt` + `originalEffective?` + `TimeSource`, exported as its own columns. `provenanceLabel` can never be empty (exhaustive switch). | It is the only shape that keeps the *original* effective value, so "edited" can show what it was edited *from*. Three loose columns make the label true but uninformative. [c3 D3, note 09 #16] | Note 01's `occurredAt`/`occurredAtSource`/`occurredAtEditedAt`; note 03's two columns; note 04's enum + two instants |
| 54 | Contradiction detection (§12.4) | Pure functions returning `List<Warning>`. **There is no `warnings` column and no `fix()` method.** The mechanism is *absence*: a warning cannot be persisted because there is nowhere to persist it, and cannot mutate because it holds no writer. Declared birth type and attached lamb count are both preserved verbatim; the mismatch is a non-blocking badge fed by a view. | Spec §12.4. The return type itself refuses to express "fix it". [note 09 #15, note 01 §5] | A trigger that corrects; an `Either<Corrected, Flags>` |
| 55 | `normalize*` ban scope | Scoped to functions that **return a corrected domain value**, not to projections. `tagDigits` alongside `tag` is not a violation — the typed value is preserved verbatim. | Note 04's blanket source ban on `normalize*` in `lib/domain` fires on the helper. [c3 F] | A blanket `normalize*` ban |
| 56 | Units | **Canonical mass = integer grams. Canonical temperature = integer milli-°C.** Non-transparent `extension type`s. Convert only at the display edge; the form is seeded from canonical and parses the *typed text* back to canonical. No `unit` column on a measurement. **Ruled 2026-08-01 (§7.0 row 11): no v1 table stores a temperature, and `app_settings.temperature_unit` is dropped with it — but `MilliCelsius` still ships**, because the measured reason it exists (0.1 °C silently rewrites 89 of 201 °F entries) is independent of whether a column uses it today. Do not delete the extension type as part of dropping the setting. | Measured: storing 0.1 kg silently rewrites **132 of 241** lb entries at 1 dp; storing 0.1 °C rewrites **89 of 201** °F entries. Both are §12.4 violations produced purely by a storage choice, in 55% of possible entries, invisible in code review. [note 09 #4, §2.2] | `decimal`/`fixed` (`BigInt` allocations for a problem integer grams solves exactly); floats; a display-unit round trip |
| 57 | Numeric input | **The in-app 60×60 pt keypad is used for weights too**, with one decimal key that always emits `.`. Free-text numeric fields reject ambiguity (`4,3` with both separators) rather than guessing. | `double.parse('4,3')` throws; `NumberFormat.parse` for a comma locale throws on `'4.3'` — worse, because a UK shepherd's phone may be set to French. Guessing is a §12.4 violation. [note 09 §2.4] | The OS numeric keyboard for weights |
| 58 | Statistics return type | **The view produces the raw counts; a pure Dart function assembles a `StatResult`** carrying `value` (`double?`), `definition`, `numerator`, `denominator`, `caveats`, `notComputableReason`. `value` is never `0` as a stand-in for unknown. | Measured: the same season yields **120% / 100% / 80% / 200%** under four legitimate definitions. A SQL view cannot carry `caveats` or `notComputableReason`, so note 01's "compute on read, expressed as drift views" reads as a complete answer and is not. [c3 D5, note 09 #9] | Nullable doubles straight out of a view; `?? 0` anywhere in `season/` or `flock/` |
| 59 | Statistic definitions | Default lambing % = **lambs born alive ÷ ewes put to the ram** (owner-confirmable, see §7). Average litter size is **always lambs born ÷ ewes lambed** and is not configurable. Barren rate counts **only ewes explicitly marked barren**. Assisted rate excludes unscored lambings from **both** sides and reports coverage. Losses bucket `stillborn` separately from "died at age 0" and distinguish `unattributed` from the user-pickable "unknown". Lambing spread is dense/zero-filled, grouped by the denormalised local civil date, anchored on the first lambing, with "ewes lambed in the first 17 days". | AHDB's house convention; Sheep Genetics: *"a blank score indicates the lambing ease was not scored."* Inferring barren from absence of data is a silent inference about a commercially sensitive number, and at 3am absence overwhelmingly means "not recorded yet". [note 09 Part 4] | `(ewesToRam − ewesLambed) / ewesToRam`; blank ease = "unassisted"; falling back to `ewesLambed` when `ewes_to_ram` is blank |
| 60 | Aggregate query mechanism | Use `customSelect` with an explicit `readsFrom:` for anything involving `GROUP BY` or date bucketing. **Do not** rely on `groupBy` inside a Dart-defined drift `View`. | drift's Dart-views page documents exactly one shape and says nothing about `groupBy` or `where` inside `as()`; note 01's snippet also leans on cascade precedence so the return value is the `JoinedSelectStatement`, not `groupBy`'s result. This is the query behind the Season Summary and the ewe-card summary line. [c2 §10] | Note 01 §10.3's `..groupBy([ewes.id])` view |
| 61 | Terminology | **09's shape, 03's storage, 10's placeholder rule.** A closed `enum AnimalClass` (stable keys, in the DB/CSV/JSON) + a user-editable `TermLabel(singular, plural)` overlay stored in a `TerminologyOverrides` table. Seeding happens in a `lib/features/settings/` bootstrap that already has a `BuildContext` — never in `domain/` or `data/`. **No domain noun appears literally in any ARB message**; every such message takes the term as a placeholder. A locale change or app update never overwrites a user's term. | NSA's own glossary shows these terms overlap and conflict (shearling by dentition, hogget by age, teg by year) — there is no true taxonomy to canonicalise to. 10's "seed from ARB defaults" would require domain or data code to import `AppLocalizations`, which the layer rules forbid. [c3 D6, note 09 #14, note 10 #12] | Terminology in `lib/domain/`; seeding from `data/`; nouns baked into ICU plural messages |
| 62 | Non-regulatory disclaimer (§12.3) | `Disclaimers.exportFooter` is a `const` in exactly one file, **referenced and never re-typed** anywhere — including by the banned-phrase allowlist — and guarded by a test. It appears in every CSV trailer, every PDF footer, and on the Export screen. | Rule 3. The guard caught a real duplication while note 09 was being written. [note 09 #19] | Re-typing the string per format |

### F. Features that needed a ruling

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 63 | Reminder scheduling | **Never `zonedSchedule()` on write.** The reminder row is written to SQLite **in the same transaction as the lambing**; one idempotent `reconcile()` projects the soonest **56** (iOS) / **200** (Android) into the OS by `cancelAll()` + rebuild. Called from exactly four places: app start after DB open, `AppLifecycleState.resumed`, after any write touching `Reminder`/`Lambing`/`Treatment`/interval settings, and after a notification tap. Debounced to once per 500 ms, off the paint frame. | Apple's hard limit is 64 pending requests per app and the failure mode at 65+ is undefined (three conflicting descriptions, one issue closed `not planned`). A 400-ewe flock generates ~500 pending reminders — naive scheduling is *structurally broken*, and it breaks by silently dropping reminders. Note 01's invariant is preserved **better** here, because the row is a durable fact and the OS projection is a rebuildable cache. [c3 C8, note 06 §1.4] | A platform-channel round-trip inside a drift write transaction; `matchDateTimeComponents`; a diff instead of teardown-and-rebuild |
| 64 | Reminders screen | Grouped due today / overdue / upcoming. The DB is the truth. The screen shows the honest line: *"Showing the next 56 reminders on your lock screen. All 312 are stored in the app."* Completing a reminder writes the domain fact (see #43) and re-reconciles. | The OS list and the app list *deliberately disagree*; that reconciliation is a screen's worth of design and currently has two incidental sentences in 14,000 lines. [c4 §3] | Silence about the discrepancy |
| 65 | Notification channels | One Android channel per reminder type (`colostrum`, `navel`, `turnout`, `tag_by`, `dose`, `withdrawal`), IDs frozen at release. No custom sound, no badge count. Permission is requested the **first time the user creates a reminder**, never at first launch. | Spec §5: "no notification permission nags mid-season". An unfamiliar sound at 3am is worse than the familiar one. [note 06 §1.1, §1.6] | Requesting permission in `initialize()`; `USE_EXACT_ALARM` (Play policy rejection for this app category) — use `SCHEDULE_EXACT_ALARM` only |
| 66 | Pen timer | **One app-level, boundary-aligned ticker at 60 s.** | A grid where cells update at different moments reads as noise under a head torch; display granularity is hours, so 30 s buys nothing; a `Timer.periodic` per row is 30 timers and measurable overnight battery. [c3 D9] | Note 01's 30 s; note 05/08's per-widget `Stream.periodic(1 min)` |
| 67 | "In the pens" list (§7.1) | The **same `PenOccupancies WHERE exited_at IS NULL` projection the Pen Board watches**, ordered by `entered_at` **ascending**, ewes only — lambs are one tap away from the dam. At frame 1 it renders the same fixed-height dark placeholder as the recents strip. | The ewe penned longest is the one most likely to need turning out and the one you are most likely to be standing next to. It shares the Pen Board's query, so it costs one extra `select`, not one extra table. [c4 §17] | A second table; sorting by pen label |
| 68 | Recents strip | A real `ewe_touches(ewe, touched_at)` table, written by the repository whenever any feature touches a ewe. | "Touched" includes looking at a ewe card without writing anything, so it is an observation, not derivable. [note 01 §10.5] | Deriving recents from event tables |
| 69 | Undo | **No generic `undo`.** Defined per verb in the repository: `beginLambing` → hard delete, allowed only while zero child rows and only within the SnackBar's lifetime; `addLamb` → hard delete; `foster` → a **compensating `FosterEvent` labelled "corrected"**, visible in history; `treatment` → **soft-void** with a `voided_at` column so the medicine book shows the void rather than losing the row; season deletion → **no undo**, guarded by the only `canPop: false` flow in the app. The window is until the SnackBar is dismissed or the route pops, whichever is first. **Undo does not survive process death, and the UI must not imply it does.** | `undo(` appears exactly once in 14,000 lines — the call site. `FosterEvents` is append-only and `birth_dam` is immutable by trigger, so a foster undo *must* be a compensating event. A treatment may already have been printed into a medicine book. [c4 §12] | A generic `repo.undo(id)`; swipe-to-delete (banned by spec §5) |
| 70 | Season summary chart | **Hand-rolled `CustomPainter`**, ~120 lines, with `semanticsBuilder`. Golden-tested at three data shapes: one day, a tight 18-day spread, a 60-day straggle. 18 pt minimum labels, no hover tooltips, ≥60 pt tap target per bar; if bars get thinner, the chart scrolls horizontally inside its card rather than shrinking. | One chart, no interaction, no animation, fixed dark palette, an existing accessibility design that assumes `semanticsBuilder`, and a hard requirement that nothing enters `pubspec.yaml` without passing the offline allowlist. Naming the rejected alternative matters: it was never *considered*, not considered-and-rejected. [c4 §16, note 09 §4.3] | `fl_chart`, `syncfusion_flutter_charts`, `graphic` |
| 71 | Empty states | A table in the design doc: one row per screen, the empty copy, and the single action it offers. Rules: never a spinner, never an illustration, never a multi-step tour; the empty state occupies the same box the content will occupy so nothing shifts; the action is the same 60 pt control the populated screen uses. **Quick Entry is never empty** — the keypad is usable at frame 1 with no data. | Spec §5 bans onboarding after first run, so **the empty states *are* the onboarding** and are the only place the app may teach anything. [c4 §11] | An onboarding wizard; illustrations; spinners |
| 72 | End-of-day export prompt | An **in-app banner on next launch**, not a notification. Add `lastExportedAt` and `lastExportPromptedAt` (both instants) to `AppSettings`. Shown at the top of Quick Entry on the first launch of a *local civil day* when: writes occurred since `lastExportedAt`, `lastExportPromptedAt` is not today, and `exportPromptDismissedForSeason != currentSeason`. Two 60 pt actions: "Export now" and "Not this season". It never appears mid-entry and never blocks. | A notification needs `POST_NOTIFICATIONS`, which is deliberately deferred to the first reminder — so a user who never creates a reminder would never get the one prompt the spec calls a *safety* feature. A prompt after a quiet day is the definition of a nag. [c4 §13] | Note 06's 20:00 local notification |
| 73 | Restore | **Replace everything, atomically.** Import into a *new* SQLite file beside the live one, validate row counts and `PRAGMA foreign_key_check`, then swap paths and reopen. Never merge into the live DB. Refuse a backup whose `schema` is **higher** than the app's own, with a clear message. Unknown columns from a newer backup are preserved into a `_unknown` JSON column. The restore screen states plainly what will be destroyed and requires a typed or two-step confirmation. | It is the single most destructive operation in the app and the only recovery path that exists. A merge UI at 3am is a data-loss generator. Guessing at a newer schema is §12.4 applied to restore. [note 06 §6.3, note 03 §5, c4 "Also worth attention"] | Merge-on-`uid` as a user-facing option; two restore code paths |
| 74 | Seed / demo data | `tool/seed.dart` writing a deterministic database **through the same restore path** as the user-facing JSON import: `dart run tool/seed.dart --ewes 400 --seasons 3 --seed 42`. Two committed fixtures: `test/fixtures/flock_400_3seasons.json` and `flock_15_at_cap.json`. Fixed seed, fixed clock. Guarded by `assert()` + a `--dart-define` so it cannot run in release. | Four topics depend on populated state (400-ewe profiling, the overflow matrix, goldens, the at-cap monetization tests) and the §7.7 recall feature needs a multi-season history that by definition does not exist yet for anyone. Routing it through restore makes the seed script a continuous test of the one code path where a bug loses five seasons. [c4 §10] | Ad-hoc fixtures per test; a generator nobody understands in season three |

### G. Platform integration

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 75 | Tag OCR (spec §7.1) | **Cut entirely, both platforms.** Recorded as a deliberate spec deviation, not silence. | ML Kit's bundled artifact still transitively depends on `play-services-base`/`basement`, which contribute `INTERNET` + `ACCESS_NETWORK_STATE` — it would trip the app's own CI gate. +~38 MB on iOS against a sub-20 MB budget. ML Kit sends usage metrics to Google by Google's own terms. An iOS-Vision-only channel would ship a platform-asymmetric feature into a spec that describes one product. Spec §7.1 marks it "Optional… never the only route", so cutting it costs nothing contractual. [c3 C7, B3 #3] | `google_mlkit_text_recognition`; iOS-Vision-only OCR |
| 76 | Voice tag entry (spec §7.1) | **Cut from v1.** Recorded as a deliberate spec deviation with its reason. **Ship the voice *note* instead** (`record`, `.m4a`, AAC-LC). | Recognition runs in *another process* whose network access our manifest cannot constrain — the "no INTERNET permission" defence, airtight everywhere else, is **useless here**. `SpeechListenOptions.onDevice` defaults false and silently falls back to network recognition, and no API reports on-device availability. [c3 B3 #4, note 06 §2.1, c1 §4.3] | `speech_to_text` 7.4.0 in v1 |
| 77 | Camera / photo | **`image_picker` using the system camera UI and system photo picker.** `flutter_image_compress` downscales at capture (`keepExif` defaults false → GPS stripped). Use `retrieveLostData()`. | `image_picker` merges **zero** Android permissions. The `camera` plugin merges `CAMERA` + `RECORD_AUDIO` and requires manual lifecycle handling for a preview surface we do not need. [note 06 §4.1, c1 §4.1] | `camera` 0.12.0+2; `permission_handler` |
| 78 | Runtime permissions | **No permission package.** Every permission this app needs has a first-party request API on the plugin that needs it. | `permission_handler`'s CocoaPods `PERMISSION_*` macro block is an App Store rejection risk, and it declares permissions the app does not use. [note 06 §9, c1 §4.3] | `permission_handler` 12.0.3 |
| 79 | Keep screen awake | **`wakelock_plus`, default-OFF Settings toggle, session-scoped, 30-minute auto-expiry, released on `dispose` and on any non-resumed lifecycle state.** | Screen-on time is the dominant battery cost and the shed is cold; but a mid-lambing sleep forces a wet-gloved unlock that alone blows the 15 s budget. The merge takes note 08's expiry (the safety-relevant half) and note 06's release condition (the stricter half). [c3 D10, note 06 §8, note 08 §8.2] | Always-on; the discontinued `wakelock` |
| 80 | Share sheet | **`share_plus`, `SharePlus.instance.share(ShareParams(...))`.** Always pass a file **path**, never `XFile.fromData`. `sharePositionOrigin` is required on iPad. | The static `Share.*` API is deprecated; omitting `sharePositionOrigin` on iPad "may cause crashes or unresponsive UI" per the README. [note 06 §6.4, c1 §4.1] | The static `Share.shareXFiles`; `open_filex` |
| 81 | File import | **`file_selector`** (`ACTION_OPEN_DOCUMENT` / `UIDocumentPickerViewController`) — no storage permission on either platform. Accept `application/octet-stream` as well and **validate magic bytes ourselves**. | `file_picker`'s cloud/Drive picking actively undercuts the positioning, and Android MIME filtering for `.zip` is unreliable. Better to accept too much and reject clearly than to grey out the user's own backup. [note 06 §7, c1 §4.3] | `file_picker` |
| 129 | Application id / bundle id | **One string on both stores, chosen 2026-08-01 and never changeable: see `RELEASES.md`'s header.** It carries no underscore and no hyphen, because an Apple bundle id admits `[A-Za-z0-9.-]` and an Android application id admits `[A-Za-z0-9_.]` — the set the two stores share is alphanumeric and the period. The literal lives in exactly three places: `android/app/build.gradle.kts` (`namespace` and `applicationId`), the Xcode target's `PRODUCT_BUNDLE_IDENTIFIER` in Debug, Release **and** Profile, and `RELEASES.md`'s header. This row records the **decision** and where the literal lives; it does not re-type it in a second spelling. | `13 §3.1`: *"chosen once, before the first upload, and can never change on either store"* — every future Play listing, App Store record, signing identity and purchase entitlement keys on it. `flutter create --org` derives two different spellings by default (`shed_book` on Android, `shedBook` on iOS) and two spellings of one identifier is the failure this row exists to prevent. [N00-T01] | Two platform spellings of one id; an id containing `_` (invalid on Apple) or `-` (invalid on Android); changing it after the first upload, which is a new app with no users |

### H. Export, PDF, CSV, backup

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 82 | CSV | **Hand-rolled RFC 4180 writer**, ~50 lines: UTF-8 BOM on, CRLF, quote on `,` `"` CR LF `;` TAB and any leading/trailing whitespace, `""` escaping, `.` decimal separator always, and a **formula-injection guard** (`'` prefix on a leading `= + - @`). Three shapes: `lambs.csv`, `ewes.csv`, `treatments.csv`, each with the §12.1 + §12.3 trailer rows. The guard is a documented transformation of the **export**, never of the record. | `csv` 8.0.0 is a fresh breaking rewrite by an unverified uploader, for ~50 lines you need byte control over. Excel on Windows mis-decodes BOM-less UTF-8, mangling every `°`, `£` and Welsh/Irish name. A note beginning `-2 lambs` becomes an executable formula in Excel and Sheets. [note 06 §6.1, c1 §4.3] | `csv` 8.0.0; Excel's proprietary `sep=;` sniffing line |
| 83 | PDF | **`pdf` only, delivered through the share sheet.** **Ruled 2026-08-01 (§7.0 row 16): there is no in-app print dialog, so this row is now closed rather than provisional** — `printing` is not re-admitted, and the OS Print action reached from the share sheet is the whole of the printing story. **Always embed a TTF; never a base-14 font.** Build on a background isolate with `compute()`; write straight to a temp file; never embed photos in the season PDF; split into `flock-book-ewes` / `flock-book-lambs` / `medicine-book` rather than crashing. | `printing` 5.15.0 depends on `http >=0.13.0 <2.0.0` and hands every future contributor a one-liner (`PdfGoogleFonts…`, `networkImage`) that quietly turns the app into a networked app on iOS, where there is no permission gate to stop it. Base-14 fonts are Latin-1 and **throw** on the curly quotes, en-dashes and ellipses an iOS keyboard inserts automatically — a crash while exporting the medicine book for a vet is a catastrophic failure of the one safety feature the app has. `save()` materialises the whole document; a 60–120 page season book is plausibly 100–200 MB peak heap. [note 06 §6.2, c3 B3 #5] | `printing`; `PdfGoogleFonts`; `Font.helvetica()`; photos in the season PDF |
| 84 | Backup / restore format | **JSON is *the* backup and *the* restore format** (spec §7.9 names JSON only). `VACUUM INTO` is demoted from "backup" to what it actually is: the mechanism for producing a `.sqlite` snapshot for the **Diagnostics / "save a copy of the damaged file"** path. **Never `File.copy` the database.** | JSON is cross-device, inspectable, and the only format that survives a schema change between the exporting and importing app versions. Two restore code paths is two migration surfaces and one of them has no spec requirement behind it. In WAL mode the DB is three files; a naive copy produces, best case, a database missing the last few hours of a lambing night. [c3 D2, note 03 §8] | `VACUUM INTO` as the user-facing backup; `File.copy` |
| 85 | Media in the backup | **Records-only for v1.** The export screen says so in plain words; media is offered as a separate share. | A 200 MB ZIP through a share sheet is a bad experience, and the records are what is irreplaceable. **Gate:** `ZipFileEncoder` does exist in `package:archive/archive_io.dart` with `OutputFileStream`, but its incremental-write behaviour is still unverified — confirm empirically before any media-in-ZIP design is attempted. [c1 LOW-16, note 03 §10.6, note 06 §6.3] | Base64 media inline (130 MB → ~175 MB inside one JSON string, built in memory) |
| 86 | Export gating | **Export is never gated by the free tier, ever.** | Spec §7.9 calls export "a safety feature, not a convenience". Paywalling the only backup mechanism in an app with no cloud is a data-hostage pattern. [note 07 §3.1] | Any export paywall |

### I. Monetization, entitlement, store compliance

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 87 | Business model | **Free app + one non-consumable IAP unlock** (`in_app_purchase`). One binary, one bundle ID, one store listing per platform. No flavors. | It is the only model that keeps the spec's free tier *and* keeps the release manifest free of `INTERNET`. Two SKUs is a direct hit on App Review 4.3(a), and — decisively — a second app sandbox destroys the retention thesis, because the shepherd's first season does not follow them. [note 07 §1.5] | Paid up front; two SKUs; subscriptions (ever); `purchases_flutter` (mandatory third-party network path + API key); `flutter_inapp_purchase` |
| 88 | Entitlement | **A locally-persisted row in SQLite**, written once, **never revoked by the app**. Excluded from the JSON backup and ignored on import. Never in `shared_preferences`. The store is consulted on exactly two explicit user actions: Unlock and Restore. Restore sits **above** Unlock on the screen. | `Transaction.currentEntitlements` and `queryPurchasesAsync` both need the network to populate their caches — unacceptable on the launch path of a 3am app. Re-locking a shepherd on night nine because a refund propagated is unacceptable against €12 of revenue. Restoring your neighbour's backup must not unlock your app. [note 07 §2] | Store calls on the launch path; entitlement in prefs or in the backup |
| 89 | Purchase acknowledgement | Set `purchase_in_flight_at` when `buyNonConsumable` is called; on next launch, **only if** it is set and `unlocked = 0`, initialise the billing client and drain the purchase stream. Also drain when the Unlock/Restore screen opens. `completePurchase` runs for purchased/restored/error on every platform. | Google auto-refunds and revokes entitlement if a purchase is not acknowledged **within three days**, and the dangerous case is the one Google names: the user pays, Play confirms, and the phone drops off the network before our app hears about it. Bounded, rare, self-clearing. [note 07 §2.3] | Initialising billing on every launch; skipping `completePurchase` |
| 90 | First frame vs entitlement | **The first frame renders the unlocked-neutral Quick Entry shell.** No screen on the 3am path ever branches on `unlocked`, so a late entitlement read is unobservable. Enforced by a widget test: no upgrade widget renders on Quick Entry, Lambing Entry, Lamb Card, Foster or Pen Board with `unlocked: false, ewesInCurrentSeason: 99`. | Decisions #21 and #88 are compatible only if the first frame is entitlement-agnostic — which it is, and which nobody wrote down. The failure mode is a paywall flash at 3am. [c3 C6] | Reading the entitlement before `runApp` |
| 91 | Free-tier cap | **One policy object consulted by the repository, with `EntryContext` as an explicit parameter.** `EntryContext.liveEntry` is structurally incapable of returning `BlockedByCap` — it returns `Allow(overFreeCap: true)` and the row is flagged. Only two calm-UI actions are gated: adding ewe #16 from the Flock screen, and starting a second season. Rows over the cap are real rows; on unlock the flags clear in one transaction; on *not* paying nothing is ever deleted, hidden, greyed out or made read-only. | Spec §7.1: "Never block an entry to make the user go and set something up first." A repository guard that blocks `createEwe` regardless of caller is exactly the 03:20 failure that "kills the app's one promise". The cap is **not** a schema `CHECK` — that would fire on a paying user mid-lambing. [c3 D7, c4 §15, note 07 §3.2] | A UI-only check; a bare `data/`-layer guard; a schema `CHECK` |
| 92 | Upgrade affordance | **No modal, ever.** Two static rows: one pinned at the top of the Flock screen, one in Settings. Always present, in the same pixels, at 3 ewes or at 15. **Nothing monetization-related renders on Quick Entry, Lambing Entry, Lamb Card, Foster or Pen Board.** | Spec §5's "zero interruptions" is written as a shipping gate. A permanent static row converts worse than a well-timed modal; that is the deliberate trade, because this audience is described as vocally hostile and will punish an app that nags. The conversion mechanism is the season wall, not the prompt. [note 07 §3.3] | Timed modals; interstitials; self-appearing bottom sheets |
| 93 | Store privacy declarations | **"Data Not Collected"** on Apple, **"No data collected or shared"** on Play. Ship `PrivacyInfo.xcprivacy` declaring `C617.1` (file timestamp) + `CA92.1` (user defaults), and `E174.1` **only if** free disk space is actually queried. A hosted privacy-policy URL is mandatory on both stores; the full policy text also ships as static Dart strings so the in-app requirement is met without `url_launcher`. | Apple defines "collect" as transmitting off-device; Play exempts payment-service data and on-device processing. `0A2A.1`/`C56D.1` are third-party-SDK codes and must not be used. Apple 5.1.1(i) requires the link in App Store Connect **and** in-app. [note 07 §4] | Any analytics; `url_launcher` |

### J. Design system, accessibility, i18n

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 94 | Colour scheme | **Hand-author every `ColorScheme` as a literal constructor call**, with the measured WCAG ratio recorded next to each pair and a unit test that recomputes them. | Flutter 3.41 changed `onPrimaryContainer`, `onSecondaryContainer`, `onTertiaryContainer` and `onErrorContainer` for every scheme produced by `fromSeed`. In a normal app that is a cosmetic surprise; here it is a regression in a safety property no test would have caught. You cannot ask `fromSeed` for "≥12:1 on the base surface". [note 05 §1.3] | `ColorScheme.fromSeed`; Material 3 Expressive packages |
| 95 | Themes | **Four theme slots, all dark, no light theme.** `themeMode: ThemeMode.dark`; `theme` and `darkTheme` set to the same resolved object; **`highContrastDarkTheme` is a genuinely different, higher-contrast palette** and is also exposed as an in-app toggle. Base surface **`#0B0D0E`**. | 05 publishes measured ratios for its whole palette against #0B0D0E and gives a surface-*ramp* argument (elevation without outlines) that 10 does not rebut; 10's #121212 rationale is an argument against #000000, which 05 also rejects. But 05 setting `highContrastDarkTheme` to the same object while claiming to honour `MediaQueryData.highContrast` is internally inconsistent — the slot becomes dead plumbing, and `highContrast` only ever fires on iOS. [c3 C4] | `#121212`; `#000000` as the base; a light theme; `highContrastDarkTheme == darkTheme` |
| 96 | Night mode | **Ship both, labelled honestly:** *"Amber (recommended)"* and *"Deep red (best for night vision, hardest to read)"*. Implemented as a third hand-authored `ColorScheme` + semantic token set that **drops luminance as well as hue**, never a global `ColorFiltered`. Photos get a *local* `ColorFiltered` only. | Spec §5 and §7.10 both name red-shift, so substituting amber unilaterally is a spec deviation; but `#FF0000` on `#121212` is 4.69:1 for a normal observer and far worse for a protanope, while `#FFB000` measures 10.23:1. Naval/aviation research: at low instrument-lighting levels the red-vs-white advantage largely disappears — intensity matters more than colour. `ColorFilter` triggers `saveLayer`, which Flutter names as expensive. [c3 C4, F; note 05 §4; note 10 §6.4] | Amber replacing red; a global `ColorFiltered`; red-shift as hue only |
| 97 | Design tokens | **Two tiers (primitive → semantic) via a single `ThemeExtension`.** Raw `Color(0x…)`, `Colors.*` and magic sizes in widgets are build-breaking defects, caught by the rule table in `tool/check_policy.dart`. Widgets read `ShedTokens`, never `ColorScheme`. | It is the only way one widget tree serves three palettes including red-shift, and it makes the eventual decoupling of `package:material` out of the SDK a mechanical migration. [note 05 §3] | Raw literals; `custom_lint` as the gate (upstream archived) |
| 98 | Typography | Body **18 pt minimum** → override the whole M3 `TextTheme` (M3 `bodyLarge` is 16.0). Bundle **Atkinson Hyperlegible Next** (SIL OFL 1.1, 114 KB variable, ships `tnum`). **No text style exceeds `FontWeight.w700`.** In red-shift, buy stroke by raising `bodySize` 18→20 and `numeralSize` 40→44, not by bumping weight. | flutter#139712: with the Bold Text accessibility setting on, w800/w900 styles render **lighter**, at w700 — so a `weightBump: 100` walks straight into the bug, on button labels and pen-tile numerals, in exactly the accessibility mode the bug affects. `google_fonts` 8.2.0 depends on `http` and fetches at runtime by default. [c3 C5, note 05 §8, note 10 #5] | `weightBump`; w800/w900; `google_fonts`; `flutter_screenutil` |
| 99 | Text scaling | **Never clamp `textScaler` globally.** The only permitted exception is `MediaQuery.withNoTextScaling` around icon fonts. **`textScaleFactor` must not appear anywhere** — including in the theme layer. Any `TextScaler` subclass overrides `scale()` only, implements `==`/`hashCode`, and is hoisted out of `build()`. | `TextScaler.textScaleFactor` is deprecated and "will be removed in a future version"; note 05's `_AtLeast` overrides it, so the CI grep note 10 mandates fires on note 05's own recommended widget. Constructing a fresh `_AtLeast` inside `build()` makes `MediaQuery.updateShouldNotify` see a changed `textScaler` on **every** rebuild, invalidating every MediaQuery dependant in the tree. Android 14+ scales to 200% non-linearly; clamping defeats the system's own curve. [c2 §7, note 10 #4–5] | Clamping; `textScaleFactor`; `flutter_screenutil` |
| 100 | Tap targets | **60×60 pt floor; 72–88 pt for the five primary 3am actions; 16 pt minimum gap.** Enforced by `MinimumTapTargetGuideline(size: Size(60, 60), link: …)` **plus** a second geometric gate. **Premised on an unrun measurement.** The 60×60 pt floor assumes the target hardware registers a tap through a wet freezer bag; that is ledger row `ziplock_capacitance` in `docs/calendar.md`, which carries the five conditions and the pass rule. If it fails, this row is struck and re-decided in the same commit as the result. | 60 pt ≈ 9.5 mm, matching Parhi et al.'s 9.2/9.6 mm optimum for a *bare* thumb in ideal conditions — gloves and cold are worse than ideal. It exceeds WCAG AAA 44×44 and Android's 48dp, and is the single highest-leverage accessibility decision in the product. The built-in guideline silently skips nodes flush with the screen edge and nodes with no semantics at all. [note 05 §6, note 04 §6.3] | 44 pt / 48 dp; `accessibility_tools`' 48×48 default as the only check |
| 101 | Gesture ban | No swipe-to-delete, no drag, no long-press-only, no pinch, no force touch, no `Dismissible`, no `Draggable`, no `Tooltip`. Every action reachable by one simple pointer action. **Premised on the same unrun measurement as #100** — ledger row `ziplock_capacitance`. The measurement uses a single tap only, because that is the only gesture this product has; there are no swipes to save. If the bag fails, this row is struck and re-decided with #100 and #102. | Spec §5, plus WCAG 2.5.1/2.5.7. `Tooltip` requires a hover or long press that does not exist here. [note 05 §7.2, note 10 §7.2] | Swipe-to-delete "with an Undo" |
| 102 | Volume-button shortcuts | **Reopened. Not answered.** Spec §17.4 makes this contingent on whether the target hardware registers taps through a freezer bag — a **hardware test**, not a desk decision. The measurement is ledger row `ziplock_capacitance` in `docs/calendar.md`, with its five conditions, its pass rule and its consequence all written down **before** the result, so the result cannot be argued with afterwards. Note also that volume buttons are not a free fallback: on Android, capturing them outside your own foreground activity needs a media session or an accessibility service, and on iOS it is bounded by Guideline 2.5.9 — either route is a new permission or entitlement, which changes the set G0 records at N02. | Note 05 declared it answered "no" on App Store Guideline 2.5.9 grounds; that guideline is real, but the *question* the spec asks cannot be closed without the shed observation. If the bag test fails, the whole interaction model changes. [c3 §E] | Closing it from a desk |
| 103 | "Saved" proof | **Commit-then-confirm**, three redundant channels: haptic + persistent SnackBar with Undo + list mutation. Announce via a **`liveRegion` whose label is unique each time**, never `SemanticsService.announce`. No optimistic UI — the UI must not show success before the transaction returns. | `SemanticsService.announce`'s `sendAnnouncement` is a no-op on Android (`NO_ANNOUNCE` is set unconditionally); the live region only fires on `didChangeLabel()`. `HapticFeedback` silently degrades and can never be the only proof. [note 05 §10, note 10 #7] | Optimistic UI; announcements as the only channel |
| 104 | Headings | Every section heading uses **`headingLevel: 1..6`**. `Semantics(header: true)` is banned in review. Ewe Card and Season Summary get a real heading hierarchy. | `header:` became a no-op on both platforms in 3.44 and still compiles, so it fails silently. The §7.7 summary line is the retention feature; a screen-reader user must be able to jump to it. [note 10 #16–17] | `header: true` |
| 105 | Reduce motion | One `reduceMotion` resolver that **ORs** `MediaQuery.disableAnimationsOf` (Android-only) and the iOS-only `reduceMotion` flag. | Verified in engine source: `disableAnimations` is never set on iOS; `reduceMotion` is iOS-only and is not on `MediaQueryData`. This contradicts common Flutter advice. [note 10 #6] | Reading one flag |
| 106 | Colour is never the only channel | Every pen-board status, withdrawal countdown and lamb alive/dead state carries **icon + text + position** as well as colour. Verified against the Grayscale colour filter. | WCAG 1.4.1 (Level A) and Apple's "Differentiate Without Color Alone": *"If you can't use your own app in grayscale, rethink your app's design."* [note 10 #14] | Colour-coded pen tiles alone |
| 107 | Accessibility ship gate | **Apple's Accessibility Nutrition Labels**, not WCAG, are the real gate. Declare VoiceOver, Voice Control, Larger Text, Dark Interface, Differentiate Without Color Alone, Sufficient Contrast, Reduced Motion. Every declared feature must complete *all common tasks*: first run, purchase, Quick Entry, Lambing Entry, Pen Board, Treatments, Settings. Target WCAG 2.2 AA as interpreted by WCAG2ICT; skip 2.4.1, 2.4.5, 3.2.3, 3.2.4. | Apple: voluntary "to start", then required to submit new apps and updates. The EAA almost certainly does not cover a farm-record app — say so plainly in the doc so nobody spends a week producing a VPAT. [note 10 #2–3, §1.2] | A VPAT; treating WCAG as the gate |
| 108 | i18n groundwork | **`flutter_localizations` + gen-l10n/ARB on day one, shipping only `en`.** `intl: any`. `supportedLocales` lists bare `Locale('en')` **first**, then `en_GB`, `en_IE`. `required-resource-attributes: true`, `nullable-getter: false`, `use-named-parameters: true`. **Never render an all-numeric date** to a human — `d MMM y`. Numeric dates only inside CSV, alongside an ISO-8601 column. | `basicLocaleListResolution` uses `languageLocales[code] ??= locale` (first wins) — putting `en_GB` first would give US users British formats. `07/13` vs `13/07` is a silent data-corruption vector against spec §12.5. The `synthetic-package` flag is dead: *"DEPRECATED. This flag cannot be enabled and should be removed."* Cost is ~2 hours; retrofit cost is a full-app string sweep. [note 10 #8–11, §9.2] | `slang`, `easy_localization` (HTTP loader extension point + unverified uploader), `intl_translation`; `synthetic-package`; `intl: ^0.20.3` |

### K. Quality: lints, testing, CI, diagnostics, size

| # | Concern | Decision | Rationale (primary source) | Rejected |
|---|---|---|---|---|
| 109 | Lints | **`flutter_lints` + an explicit `analyzer: language: {strict-casts, strict-inference, strict-raw-types}` block.** | `flutter_lints` alone sets **no** analyzer language modes, which is what note 07 correctly calls unacceptable — but its own stated fallback gets "the 90%" without ~215 rules of noise for a solo developer. The load-bearing argument is `strict-casts`, and it is specific to this app: every row out of SQLite and every field out of a JSON backup is a `dynamic`-adjacent boundary. [c3 C11, c1 LOW-14] | `very_good_analysis` as the headline (kept as a documented alternative); `flutter_lints` with no strict block |
| 110 | Test shape | **Not a pyramid.** A thick pure-domain unit layer, a real in-memory drift layer including every migration path, a wide-but-shallow widget layer driven by the overflow matrix, ~8 goldens, 4 integration journeys, and a `test/policy/` directory that turns spec §12 into executable assertions. | Risk is concentrated in arithmetic that is invisible when wrong, a database that is the only copy of the data, and five safety rules that are product promises rather than code properties. The screens are comparatively low-risk. [note 04 §1] | Golden-testing 12 screens × the matrix; mocking drift; unit-testing PDF bytes |
| 111 | Test database | **`NativeDatabase.memory()`, never a mock.** In widget tests, `DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true)`. | drift's own testing guide: unsubscribing keeps a stream open for one event loop iteration, and "in Flutter widget tests it's illegal to keep these timers open after a test concludes". Forget this and every stream-touching widget test fails with a pending-timer error. [note 04 §3, note 01 §14.1] | Mocks; a repository interface *for testability* |
| 112 | Test doubles | **Hand-written fakes for all six gateways** (notifications, share/export, media/file system, camera, audio, clock). `mocktail` kept only for interaction-ordering and non-invocation assertions. | Overuse risk is real: hand-written fakes beat a mocking library for six small interfaces. `mockito` needs `@GenerateNiceMocks` codegen for 3–6-method interfaces. [note 04 §4, c1 §4.2] | `mockito`; `checks` (*"still experimental. For production use cases, please use `package:test` and `package:matcher`"*) |
| 113 | Time in widget tests | `AutomatedTestWidgetsFlutterBinding` already provides an **advancing** fake clock, so `tester.pump(Duration(hours: 25))` really does move `clock.now()`. **`Clock.fixed` freezes time and is only for single-instant assertions** — for elapsed-time widget tests, offset the seed data instead of pinning `now`. | Verified: the binding does `_clock = fakeAsync.getClock(DateTime.utc(2015))` and `FakeAsync.run` installs the zone clock. Wrapping a pen-board test in `withClock(Clock.fixed(...))` makes every "hours since penned" readout stay at its initial value forever — the test silently measures 0 h. [c2 §9] | Note 04 §2.3's second recipe as written |
| 114 | The 216-cell overflow matrix | **Keep it — it is the best value-per-line in the suite.** Every screen × {375×667, 390×844, 430×932} × textScaler {1.0, 1.3, 2.0} × boldText {false, true}: assert no `RenderFlex` overflow and no exception. Plus a reachability assertion: the primary action of Quick Entry, Lambing Entry and Foster must be on screen without scrolling at the smallest device × textScaler 1.3. | 216 cheap assertions from ~30 lines of table-driven code, for a product whose requirement is "legible at 18 pt in a head torch". It already covers text scale and bold text without pixels. [note 04 §5.3, c3 D8] | Text-scale goldens at 6 variants × 12 screens (72 images) |
| 115 | Accessibility gates in tests | Every `meetsGuideline` run **must** begin `final handle = tester.ensureSemantics(); addTearDown(handle.dispose);`. | Without a live `SemanticsHandle`, `semanticsOwner` is null and the traversal is a null-check throw — the gate cannot do its job. Note 05 §6.4 is the version a reader lands on first, and it omits it. [c2 §6, note 04 §6.1] | Note 05 §6.4 as written |
| 116 | Goldens | **~8 images, dark theme, tagged `golden`, pinned to one runner and one exact Flutter version.** Built-in `matchesGoldenFile` + a project `flutter_test_config.dart` that loads the real fonts and installs a tolerant comparator. **Not a per-PR gate** — a tag-triggered / manually dispatched macOS job plus a `make goldens` target run locally before tagging. | Goldens change only on deliberate re-baseline PRs, so per-PR macOS is pure cost: GitHub bills macOS at a **10× multiplier**, and the Free plan's 2,000 minutes = 200 macOS minutes/month — a per-push macOS build burns the whole quota in a week. [c3 C10, note 04 §7, note 07 §9] | `golden_toolkit` (**discontinued**); `alchemist` (CI text-blocking replaces text with coloured squares, destroying the legibility property the goldens exist to prove); per-PR macOS |
| 117 | Integration tests | **`integration_test` (SDK), four journeys, no more.** Nightly on a real device; reported, not blocking. | `patrol` requires `patrol_cli`, a Gradle and Xcode test target, and `flutter test` will not run its tests. [note 04 §8, c1 §4.3] | `patrol`; Firebase Test Lab (requires an account and an upload — the exact posture the product rejects) |
| | **AMENDED 2026-08-05 (N33-T08): the transitive cost, measured rather than assumed.** Adding `integration_test` pulls **five** packages this project had never resolved — `flutter_driver`, `fuchsia_remote_debug_protocol`, `process`, `webdriver`, `sync_http` — and **two of them open sockets**: `webdriver` speaks the W3C WebDriver protocol, `sync_http` is a synchronous HTTP client. The decision stands, and the cost is written down rather than allowlisted quietly. All five are `dev_dependency`-only and reach no shipped artefact; **G1 is what proves that** — the permission set is asserted on the shipped `.aab`, never on the resolved graph, which is the same distinction §3.1's amendment of 2026-08-01 already had to make about Play Billing. `tool/policy_allowlist.txt`'s `[transitive]` header carries the reasoning beside the lines. | The alternative was writing *"confirm it opens no socket"* beside an HTTP client, in the one file this project keeps for not doing that | — |
| 118 | Property tests | **Amended 2026-08-01: a hand-rolled seeded generator, and nothing else.** ~~`glados` for the pure value round-trips only (its shrinking earns its keep);~~ a hand-rolled seeded generator for the whole-flock export round trip. Do not extend it. Pure value round-trips are covered by an explicit table of cases in the same file, which is what the shrinking was buying and is one screen of code. | `glados` does not resolve against `drift_dev` 2.34.5 at any version (§5.2, struck). The original caution — unverified uploader, 2.5 years stale — turned out to understate it. A seeded generator nobody understands in season three is worse than a fixture, and so is a package that cannot be installed. [c1 §4.2, c3 §E; N00-T03] | `flutter_glados`; `spot`; pinning `drift_dev` down to keep a property-test package |
| 119 | Coverage | **Report, never a gate.** Track one number that means something: coverage of `lib/domain/**`, aimed high (95%+) as a review prompt. Strip `*.g.dart` from lcov. | The highest-value tests contribute almost zero line coverage — the manifest gate, the schema-JSON assertion, the source scans, the a11y runs. A percentage gate creates pressure to test `copyWith` while the DST cases stay unwritten. **Remove `*.freezed.dart` from the lcov strip** — freezed is rejected. [note 04 §9.5, c3 D10] | A coverage gate |
| 120 | Tap-budget tests | Extend note 04's tap-budget assertions from the lambing path to **`foster` and `repeat treatment`**. | Spec §7.3 calls foster "the flow most likely to be abandoned if it takes five taps" and §7.5 requires a repeat-last-treatment shortcut for batches. Both are tap-budget claims with no gate. Cheap; turns two prose claims into tests. [c4 "Also worth attention"] | Prose claims |
| 121 | CI shape | **Every push/PR on `ubuntu-latest`:** format (`--set-exit-if-changed`), analyze (`--fatal-infos`), codegen freshness (`build_runner` + `make-migrations` + `git diff --exit-code`), unit + widget + policy + migration tests with randomized ordering, `TZ=Pacific/Chatham` domain run, **release AAB build + permission assertion**. **Tag `v*`:** signed AAB artifact. **Tag `v*` or manual:** macOS goldens. **iOS is built by hand on the developer's Mac for v1.** | The macOS multiplier makes per-push iOS a same-week outage rather than a budgeting mistake. Automating a five-minute manual step costs signing-secret plumbing, `fastlane match` and the entire macOS budget. This is the opposite of the usual Flutter-CI advice and for a solo dev with a Mac it is correct. [note 07 §7, note 04 §9.4] | Per-push macOS; automated iOS release for v1 |
| 122 | Offline gates in CI | **Two mechanical gates earn their keep:** (a) `bundletool dump manifest` on the shipped release **`.aab`**, asserting the exact permission set; (b) the direct-**dependency** allowlist over `pubspec.lock`, which **must distinguish `dependencies` from `dev_dependencies`**. The manifest-merger report is kept as the *diagnostic* that names which library added a permission. An import-level scan of `lib/` bans network package imports. | Note 01's grep of `build/app/intermediates/` is unsound — that directory accumulates debug and profile artifacts, and Flutter's debug/profile manifests **do** declare `INTERNET`, so the grep fires on a stale directory. `build_runner` legitimately pulls `shelf`/`web_socket_channel` into the lockfile as dev deps, so an undifferentiated allowlist fails on day one. `HttpOverrides.global` is a runtime belt over a manifest brace that already makes sockets impossible on Android. [c3 C10, B3 #8, §E] | `build/app/intermediates/` greps; `apkanalyzer` on an APK; a fourth `HttpOverrides.global` proof |
| 123 | Crash diagnostics | **No Sentry, no Crashlytics, no analytics of any kind.** A rolling, **redacted**, plain-text log in application support (never cache), 256 KB cap, one rotation. Crash-path writes are `writeAsStringSync(..., flush: true)` and bypass any stream. A `session.lock` set at launch and cleared on `AppLifecycleState.hidden` detects native crashes, OOM kills and battery death. Settings ▸ Diagnostics shows the last 20 events, record counts, storage figures, `PRAGMA quick_check`, and a **user-initiated** share. **No automatic prompt to send.** | Spec §4.3, §4.5 and §13 each independently rule out a crash reporter — the fact that a specific device crashed at 03:41 is itself telemetry the user did not consent to. `FlutterError.onError` and `PlatformDispatcher.onError` see only *Dart* errors, which are not the failures that matter at 3am. An async write may never flush if the process is about to be killed. [note 08 §7] | Crashlytics/Sentry; automatic send prompts; unredacted logs |
| 124 | Redaction rules | **Allowed:** timestamp, app version + build, OS version, device model, free bytes, route name, exception *type*, stack trace, SQLite result codes. **Forbidden:** ewe tags, note text, product names, batch numbers, withdrawal periods, media paths, row contents. Log `SqliteException.resultCode`/`extendedResultCode` plus a statement identifier you control — **never the raw message**. Rewrite sandbox UUIDs out of stack traces. | Spec §4.5. SQLite exception messages echo SQL and sometimes bound values. The copy above the share button — *"This file contains no animal records… You can open it and read it before you send it."* — is what makes voluntary reporting actually happen. [note 08 §7.6] | Logging exception messages verbatim |
| 125 | Off-isolate work | **Only PDF generation and image downscaling.** CSV/JSON at this volume are milliseconds. A drift connection cannot cross an isolate boundary — use `computeWithDatabase` / `serializableConnection()`. `flutter_image_compress` needs `BackgroundIsolateBinaryMessenger.ensureInitialized` off the root isolate. | Measured shape, not speculation: the risk is an OOM kill during "export my season", which is precisely the moment the user is trying to protect their data. [note 08 §5, c1 §4.1] | Isolating CSV/JSON; passing a drift connection to `compute` |
| 126 | Performance measurement | **CI gates app *size*, not app *speed*.** Profile mode is disabled on emulators, so any CI perf number from a hosted runner is noise. Startup and frame timings are measured **by hand on two real devices per release** and committed to `docs/perf/measurements.md`. Budget: interactive keypad at the first frame; first Flutter frame ≤ 400 ms after `main()` on the oldest target device. | `flutter run --profile --trace-startup` writes `build/start_up_info.json`. Apple's published goal is a first frame within 400 ms; Android vitals treats cold start as excessive at ≥5 s. The 15-second budget is dominated by the human, not the machine — what kills you is a spinner before the shepherd can start typing. [note 08 §1, §10] | CI perf assertions; emulator profiling |
| 127 | App-size budget | **Spec §11 is reframed, and the deviation is stated once with an owner.** Bundled assets **< 5 MB** and no licensed data of any kind. Android AAB download target **< 20 MB**. **iOS install size will plausibly be 25–45 MB and is not promised anywhere user-facing.** One tracked total in `docs/perf/measurements.md`, updated per release from `--analyze-size` JSON archived as a CI artifact. Ship an AAB, never a fat APK; `--obfuscate --split-debug-info`, symbols kept per release. | The variable font, `flutter_localizations` + `intl`, and the plugin set all draw on the same budget with no single note tracking the total. Flutter's own docs give no 2026 baseline. [c3 D10, note 07 §5] | Reading spec §11 as a promise about install size; `--split-per-abi` for store distribution |
| 128 | Release hygiene | Build number = the CI run number, always. Build name bumped by hand in the tag; `git tag v<version>` triggers the release workflow. **Freeze releases during the customer's lambing season (roughly February–April in UK/Ireland)** — a regression shipped on 3 March costs someone a night of records. Refresh the bundled IANA timezone data as a release-checklist item. Keystore backed up somewhere that is not the laptop. | Both stores reject a re-used build number. The seasonal freeze is in the release checklist, not in someone's head. [note 07 §6, c1 §4.1] | Manual build numbers; shipping mid-season |

---

## 3. The offline-purity contract

### 3.1 What the app promises — three tiers, and only two are claimable

| Tier | Claim | Achievable? |
|---|---|---|
| 1 | **The app has no network code and no `INTERNET` permission.** Nothing in our process can open a socket. | **Yes on Android, mechanically provable.** Yes on iOS by construction and observable via the App Privacy Report. |
| 2 | ~~**No dependency attempts a network call from our process.**~~ **Amended 2026-08-01 (N02-T02): no dependency *can* reach a network from our process.** | **Yes**, once ML Kit, `speech_to_text` and `printing` are dropped. Provable by import scan + dependency allowlist + merged-manifest inspection — and *that* is what those three prove. **What they do not prove is the struck wording.** G0 found `com.google.android.datatransport:transport-backend-cct:3.1.8` in the graph, a compile-scope dependency of Play Billing 8.0.0 whose entire purpose is uploading telemetry to Google. Whether it *attempts* a call is not ours to know; whether it can succeed is, and it cannot, because the Android build has no `INTERNET` permission and the kernel does not care what a library intended. Claiming the stronger sentence would have meant claiming something about somebody else's code that no gate in this project reads. |
| 3 | **No data ever leaves the device by any route.** | **No.** The share sheet, the system photo picker and (had we shipped it) the OS speech recognizer are *other processes* with their own network access. Removing `INTERNET` from **our** manifest does not constrain them. |

**The only public wording permitted** (tier 1 + tier 2, verbatim):

> "Shed Book has no account, no server and no sync. The Android build ships without the internet permission, so the app itself cannot connect to anything. Your records only leave the phone when you deliberately export and share them."

**Never write "your data never leaves your phone."** It does, the moment they AirDrop a CSV — which is the backup story the spec depends on.

Two claims currently in the research notes must stop being made: drift has "zero network surface" (note 03 §1.1) and "the no network path claim survives Riverpod 3.x intact" (note 02 §1.3). Both are true and both are about the wrong layer. The binding claim is tier 1 + tier 2, and it is only true if the drops in §4 actually happen.

### 3.2 The mechanical gates that prove it

| # | Gate | What it proves | Blocking? |
|---|---|---|---|
| G1 | `bundletool dump manifest` on the **shipped release `.aab`** asserts the permission set is **exactly** §3.3's eight uncommented lines (~~eight entries~~ — struck 2026-08-01; G0 measured nine names and eight lines) | A plugin cannot silently merge a permission | **Yes**, every push |
| G2 | Direct-**dependency** allowlist over `pubspec.lock`, with `dependencies` and `dev_dependencies` scanned separately and a **documented transitive allowlist** | A new package cannot enter the graph unreviewed | **Yes**, every push |
| G3 | Import-level scan of `lib/`: no `package:http`, `dio`, `connectivity_plus`, `firebase_*`, `*_web_socket`, `google_fonts`, `PdfGoogleFonts`, `networkImage` | Our own source cannot reach a network API | **Yes**, every push |
| G4 | `manifest-merger-release-report.txt` archived as a CI artifact | **Diagnostic only** — names which library contributed which permission | No |
| G5 | iOS: no `NSAppTransportSecurity` key in `Info.plist`; App Privacy "Data Not Collected"; `PrivacyInfo.xcprivacy` present; one manual App Privacy Report / `nettop` check per release | There is no iOS permission to remove, so enforcement is construction + observation. **Say so honestly rather than implying parity with Android.** | Manual, per release |

**G0 — the prerequisite. Closed 2026-08-01** (N02-T01); the record is §3.3 and
[`13-build-ci-release.md`](../engineering/13-build-ci-release.md) §2.2, and the artefact is `docs/gates/manifest-merger-release-report.txt`. It stays written here because a closed prerequisite that reads as open is how the same afternoon gets spent twice, and because G1 is only as good as the day this was read.

Before any `tools:node="remove"` line is committed: run `flutter build appbundle --release`, read `build/app/outputs/logs/manifest-merger-release-report.txt`, and **record the actual permission set contributed by Play Billing 8.0.0 in this file**. Removing `INTERNET` is safe and proven — ~~and~~ **now proven twice**: the release `.aab` built with the line drops `INTERNET` and keeps everything else, and the debug and profile variants keep theirs. ~~**Removing `ACCESS_NETWORK_STATE` is NOT yet proven**~~ — **struck 2026-08-01: it is not removed at all.** Three notes hard-coded its removal, and the only AAR manifest anyone could fetch was billing **2.0.3**, six majors behind. The measured answer is that billing 8.0.0's own manifest declares neither network permission and a **transitive** Google telemetry library declares both, so §2.2's *leave it* branch is the one that fires. Had the removal been committed on faith, the failure would have surfaced as a purchase flow that misbehaves on a flaky connection, in production, on someone else's phone.

### 3.3 The permission set — nine entries, eight of them lines in the expected file

**G0 was run on 2026-08-01** (N02-T01). Everything below is read off a real release
`.aab` with `bundletool 1.18.3 dump manifest`, and attributed with
`build/app/outputs/logs/manifest-merger-release-report.txt`, archived at
`docs/gates/manifest-merger-release-report.txt`. Nothing here is remembered.

> ~~`android.permission.WAKE_LOCK  <- wakelock_plus (merged)`~~ — **struck 2026-08-01.**
> `wakelock_plus` 1.7.0 contributes **no** permission. Its manifest merges `<application>`
> attributes only, and the plugin keeps the screen on with the `FLAG_KEEP_SCREEN_ON` window
> flag, which needs none. `WAKE_LOCK` appears **zero** times in the merger report. It was
> carried from note 06 and never checked.
>
> ~~`android.permission.ACCESS_NETWORK_STATE  <- removal PENDING G0`~~ — **struck 2026-08-01.**
> It is present, it is not billing's own manifest that adds it, and §2.2's second permitted
> outcome fires: **leave it.** See the line below for what actually contributes it.
>
> ~~"eight entries, not seven"~~ — **struck 2026-08-01.** The count moved by measurement, not by
> decision: −1 `WAKE_LOCK`, +1 `ACCESS_NETWORK_STATE`, +1 `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`.

```
# AS BUILT — the exact uses-permission set the release .aab declared, 2026-08-01.
# Seven entries. Each names the manifest the merger attributed it to.
android.permission.POST_NOTIFICATIONS      <- flutter_local_notifications 22.2.0 (merged)
android.permission.VIBRATE                 <- flutter_local_notifications 22.2.0 (merged)
android.permission.RECORD_AUDIO            <- record_android, via record 7.1.1 (merged)
com.android.vending.BILLING                <- com.android.billingclient:billing:8.0.0 (merged)
com.shedbook.shedbook.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION <- androidx.core:core:1.18.0 (merged)
android.permission.ACCESS_NETWORK_STATE    <- com.google.android.datatransport:transport-backend-cct:3.1.8 (merged)
android.permission.INTERNET                <- com.google.android.datatransport:transport-backend-cct:3.1.8 (merged)
```

```
# NOT IN THE ARTEFACT ABOVE. These three are the whole of what N31-T01 and N31-T02
# change about the permission set, and the reason each is not there yet.
android.permission.RECEIVE_BOOT_COMPLETED  <- we add — reminders survive a reboot (N31-T02)
android.permission.SCHEDULE_EXACT_ALARM    <- we add — user-granted; NEVER USE_EXACT_ALARM (N31-T02)
android.permission.INTERNET                <- REMOVED by tools:node="remove" in src/main (N31-T01).
#                                             Proven on 2026-08-01: the merger logs
#                                             "REJECTED from [transport-backend-cct:3.1.8]" and
#                                             the release .aab drops to six entries.
```

**The shipped set is therefore nine names and eight lines.** Nine is this section's two blocks
resolved — the seven above, minus `INTERNET`, plus the two we add. Eight is the uncommented lines
`android/expected_permissions.txt` holds, because `INTERNET` is asserted by its *absence*. Same fact
counted two ways, and confusing them is how somebody adds a ninth line to make a red build green.

**Two libraries nobody in this doc set had listed contribute permissions, and one of them matters.**

- `androidx.core:core:1.18.0` declares `${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`,
  a signature-level permission it defines and holds itself, used by `ContextCompat.registerReceiver`.
  It is invisible in the Play listing and reaches no network. It is in the set because G1 asserts
  **exact set equality**, and a set that omits it is red on the first Android job.
- `com.google.android.datatransport:transport-backend-cct:3.1.8` is a **compile-scope dependency of
  Play Billing 8.0.0** — Google's Cloud CCT telemetry transport — and it, not
  `billing-8.0.0/AndroidManifest.xml`, is what declares both `INTERNET` and `ACCESS_NETWORK_STATE`.
  Billing's own manifest declares `com.android.vending.BILLING` and nothing else, which is exactly
  what §22 A2 guessed and could not prove. **What A2 did not anticipate is the transitive edge.**
  Billing 8.0.0 also drags in `play-services-base` 18.5.0, `play-services-basement` 18.4.0,
  `play-services-location` 19.0.0 and `play-services-tasks` 18.2.0; none of those contributed a
  permission to this build, and the location one contributing none was checked specifically.
  What this means for the tier 2 claim is **[`13-build-ci-release.md`](../engineering/13-build-ci-release.md) §2.2's ruling** — it is not a
  question this section may answer, because §3.1's wording is public copy.

Note 06's canonical list of seven was wrong the moment monetization shipped: a grep of notes 06, 01, 04 and 08 for `in_app_purchase` / `billing` / `BILLING` returns **zero hits**. The offline gate and the monetization decision were designed in ignorance of each other. The billing AAR is a **Play-Services-adjacent artifact** whose transitive Gradle graph must be reviewed on every Billing Library bump — and G0 has now shown that the graph, not the AAR, is where the permissions were hiding.

**The record above is true of one dependency set** — the `pubspec.lock` in the tree on 2026-08-01.
That is not a weakness to hedge: it is precisely what G1 exists to catch on every push afterwards.

### 3.4 The honest exceptions — things that touch a network and are kept anyway

| # | Thing | Why it is kept | How it is bounded |
|---|---|---|---|
| 1 | **`http 1.6.0` is in the dependency graph via four regular edges** — `flutter_local_notifications → timezone → http`, `wakelock_plus → package_info_plus → http`, `file_selector → file_selector_platform_interface → http` and `image_picker → image_picker_platform_interface → http`. ~~Two~~ — **struck 2026-08-01 (N03-T04): four, measured with `dart pub deps --json`.** The two nobody had listed are the file picker and the photo picker | Unavoidable; all four are load-bearing | Runtime exposure on Android/iOS is plausibly nil (`timezone`'s use is in `browser.dart`; `package_info_plus`'s is on web), and a built APK shows no `INTERNET` permission and no surviving network symbols after AOT. **But any "no http in pubspec.lock" gate is unsatisfiable and must not be written.** The gate is G1 + G2 + G3. |
| 2 | **`in_app_purchase`** | Spec §14 requires a trial; only IAP delivers it without a second app sandbox | Binder IPC to the Play Store / XPC to the App Store daemon — *someone else's* socket. Called **only** on explicit Unlock / Restore taps, never on the launch path. Adds `com.android.vending.BILLING`. |
| 3 | **`sqlite3` build hooks download prebuilt binaries from GitHub at build time**, sha256-verified | Bundling SQLite is what guarantees `STRICT` + FTS5 on every device | Build machine only; the shipped app has no network code. **Documented in `README.md`** as of 2026-08-01 (N02-T01), with the measured answer to §22 B20: `pub get` first on an empty pub cache, then **`flutter test`**, which fetches the host `libsqlite3.dylib`; `flutter build appbundle` fetches three more, one per Android ABI, so warming one target does **not** warm the other. The cache is `.dart_tool/hooks_runner/shared/`, which `flutter clean` deletes — so `flutter clean` is genuinely cold for the hook, unlike for `~/.pub-cache`. The build is not hermetic; vendor the pub cache or use `url_pattern` to point at a mirror if a 2029 rebuild matters. |
| 4 | **`build_runner` pulls `shelf` / `web_socket_channel` into `pubspec.lock`** | Dev-only, never shipped | G2 scans `dependencies` and `dev_dependencies` separately. |
| 5 | **A hosted privacy-policy URL** | Mandatory on both stores; the one piece of internet infrastructure this project cannot avoid | It is *outside* the app. No `url_launcher`; the full policy text also ships as static Dart strings. |
| 6 | **The share sheet, the system photo picker** | The share sheet *is* the export mechanism (spec §7.9) | Tier 3 is explicitly not claimed. |

---

## 4. Dropped, degraded, or opt-in gated — the blunt list

| Spec clause | Fate | Why, in one line |
|---|---|---|
| §7.1 "Optional tag OCR using the OS text recogniser" | **DROPPED, both platforms** | ML Kit's transitive `play-services-base`/`basement` contribute `INTERNET` + `ACCESS_NETWORK_STATE` and would trip the app's own CI gate; +38 MB on iOS; ML Kit sends usage metrics to Google. An iOS-only channel would ship a platform-asymmetric feature into a one-product spec. |
| §7.1 "Optional voice tag entry using OS on-device speech recognition" | **DROPPED from v1** | Recognition runs in another process whose network access our manifest cannot constrain, and `onDevice: true` silently falls back to network recognition with no API to detect it. **Degraded to:** the voice *note* (§7.2), which is pure local recording. |
| §7.9 "PDF flock book… printable" | **DEGRADED** | No in-app print dialog. `printing` depends on `http` and hands contributors a one-line footgun. Delivery is share sheet → the OS Print action. |
| §7.9 "Full JSON backup for restore onto a new device" | **DEGRADED to records-only for v1** | Media is a separate share. A 200 MB ZIP through a share sheet is a bad experience; the records are what is irreplaceable. Blocked on verifying `ZipFileEncoder`'s incremental-write behaviour before any media-in-ZIP design. |
| §7.9 "gentle end-of-day prompt to export" | **DEGRADED to an in-app banner** | A notification would need `POST_NOTIFICATIONS`, which is deliberately deferred to the first reminder — so a user who never creates a reminder would never get the one prompt the spec calls a safety feature. |
| §5 "Optional red-shift mode" | **PRESERVED but re-defaulted** | Both ship. Amber is labelled "(recommended)" and red "(best for night vision, hardest to read)", because red-on-black is one of the worst combinations for protanopia. Substituting amber outright would have been an undeclared spec deviation. |
| §11 "Total app payload well under 20 MB" | **DROPPED as an install-size promise** | Reframed to: bundled assets < 5 MB, no licensed data, Android AAB download < 20 MB. **iOS install will plausibly be 25–45 MB.** The binary is what Flutter costs. Needs an owner. |
| §7.6 "All intervals user-configurable; all reminders individually mutable" | **PRESERVED, but the OS view is windowed** | iOS caps at 64 pending requests and the failure mode above that is undefined. Only the soonest 56 reach the lock screen; all of them live in the app, and the Reminders screen says so. |
| §17.4 volume-button shortcuts | **REOPENED, not answered** | Note 05 closed it from a desk on App Store Guideline 2.5.9 grounds. The spec makes it contingent on a hardware test through a freezer bag. If the bag test fails, the interaction model changes. |
| §12 "visible in the code review checklist" | **PRESERVED, as a deliverable** | No file in the corpus produces that checklist; it is now a named artifact (`CODE-REVIEW-CHECKLIST.md`). |
| §11 "roughly 40 authored terms… written from scratch" | **AT RISK — must be written, not copied** | Actual state: ~13 of ~40. **0 malpresentations, 0 treatment routes.** Worse, note 09 says of the SRUC ease scale *"Adopt them verbatim"* — verbatim adoption of a levy-body technical note is the opposite of "written from scratch" and pressures "None that is licensed". The cited PDF is image-based and its text and licence terms **could not be verified**. Paraphrase at the same semantic granularity; author the two missing lists; add a provenance line per list and a "no verbatim third-party copy in `assets/content/`" CI check. |
| §15 success criteria 1 and 4 | **NOT INSTRUMENTABLE — declared qualitative** | "A shepherd uses it on night two and night eleven" and "at least one user opens a previous season" are cross-user retention facts; a local, never-transmitted Diagnostics screen structurally cannot produce them. Bound instead to the 12-tester beta cohort (a two-question post-season note) plus a **local, opt-in** Diagnostics line — "You have recorded on 9 nights this season" — which transmits nothing. |
| — | **Entitlement is never revoked** | Detecting a refund requires polling the store on the launch path. Re-locking a shepherd on night nine against €12 of revenue is unacceptable. Deliberate and documented. |
| — | **No telemetry of any kind** | Accepted consequence: you will never know why a shepherd stopped using the app. The compensating mechanism is the diagnostics export plus direct contact with the forums in spec §3. |
| — | **State restoration dropped** | Restoring a stale selected ewe at 3am is a data-integrity bug. Also deletes an easy-to-miss iOS storyboard step. |
| — | **Bundled IANA snapshot goes stale** | Accepted. `package:timezone` is confined to the notification seam precisely so the rest of the app uses the OS zone, which after three unupdated seasons is *more* correct than a bundled snapshot. |

---

## 5. The verified dependency table

**Source: the c1 audit, verified against the pub.dev API on 2026-07-27, and checked to resolve against Flutter stable's `meta: 1.18.0` / `test_api: 0.7.11` / `intl: 0.20.2` pins.** No version in this project may come from any other source, including memory.

### 5.1 Runtime dependencies

| Package | Version | Publisher | Why it is here | What it costs |
|---|---|---|---|---|
| `drift` | 2.34.2 (2026-07-14) | simonbinder.eu ✓ | Reactive typed SQLite; `watch()` makes "the DB is the single source of truth" structural; generated migration snapshots + `SchemaVerifier` | build_runner in the loop; single maintainer; open #3338 (torn `combineLatest`), #3295 (no `distinct()`) |
| `drift_flutter` | 0.3.1 (2026-07-11) | simonbinder.eu ✓ | `driftDatabase()`; background connection; sets `sqlite3.tempDirectory` | Pre-1.0. Defaults to Documents — **override to Application Support**. Drags the `sqlite3_flutter_libs`/`sqlcipher_flutter_libs` EOL stubs into the lockfile (harmless, unavoidable) |
| `sqlite3` | 3.5.0 (2026-07-18) | simonbinder.eu ✓ | Bundles SQLite via Dart build hooks; guarantees FTS5 + STRICT on every device | Build hooks **download** binaries from GitHub at build time (sha256-verified). Build machine needs network; shipped app does not |
| `path_provider` | 2.1.6 (2026-06-15) | flutter.dev ✓ | DB + media + temp paths | iOS container UUID changes across installs — **never persist absolute paths**. Apple privacy-manifest SDK |
| `uuid` | 4.6.0 (2026-07-15) | yuli.dev ✓ | RFC 9562 v7 ids; monotonic prefix keeps the uid index appending | None material. Pure Dart |
| `share_plus` | 13.3.0 (2026-07-23) | fluttercommunity.dev ✓ | The only export/backup channel (spec §7.9) | Static `Share.*` deprecated — use `SharePlus.instance.share(ShareParams(...))`. `sharePositionOrigin` required on iPad. Requires Flutter ≥3.38.1 |
| `clock` | 1.1.2 (2024-10-28) | tools.dart.dev ✓ | `clock.now()` / `withClock()`; the chokepoint that makes spec §12.5 testable | Must be paired with the CI ban on `DateTime.now(` and SQL-side time or it is bypassed |
| `intl` | **0.20.2** (SDK-pinned) | dart.dev ✓ | `DateFormat`/`NumberFormat`; transitive requirement of `flutter_localizations` | **Must be declared `intl: any`** — `flutter_localizations` pins 0.20.2 exactly, so `^0.20.3` will not resolve. Re-check after every Flutter upgrade |
| `flutter_localizations` | SDK | flutter.dev | `GlobalMaterialLocalizations` → en-GB date/time pickers | Pins `intl` exactly. Not on pub.dev |
| `flutter_local_notifications` | 22.2.0 (2026-07-25) | dexterx.dev ✓ | The only reminder mechanism (spec §7.6) | Merges `POST_NOTIFICATIONS` + `VIBRATE` only; you add `RECEIVE_BOOT_COMPLETED` + `SCHEDULE_EXACT_ALARM`. **Drags `timezone` → `http 1.6.0` into the graph.** Heavy breaking-change history (v16/v18/v19/v20). Requires Flutter ≥3.38.1, `desugar_jdk_libs` 2.1.4 |
| `timezone` | 0.11.1 (2026-06-29) | labs.dart.dev ✓ | Forced by `zonedSchedule`. Confined to that seam | **Declares `http: ^1.6.0` as a regular dependency.** Bundled IANA 2025c ages; refresh is a release-checklist item. 0.11.0 changed `Location.offset` `int`→`Duration` |
| `wakelock_plus` | 1.7.0 (2026-07-21) | fluttercommunity.dev ✓ | Screen sleep mid-lambing forces a wet-gloved unlock — alone blows the 15 s budget | **Drags `package_info_plus` → `http 1.6.0` into the graph.** Per-screen, default off, released on dispose and on any non-resumed lifecycle state |
| `image_picker` | 1.2.3 (2026-06-30) | flutter.dev ✓ | Photo attach via system photo picker / system camera | Merges **zero** Android permissions. Needs `NSCameraUsageDescription` etc. Use `retrieveLostData()` |
| `flutter_image_compress` | 2.5.1 (2026-07-25) | fluttercandies.com ✓ | Native downscale at capture; `keepExif` defaults **false** (strips GPS) | Needs `BackgroundIsolateBinaryMessenger.ensureInitialized` off the root isolate. `quality` ignored for PNG on iOS |
| `file_selector` | 1.1.0 (2025-11-21) | flutter.dev ✓ | Backup import via `ACTION_OPEN_DOCUMENT` / `UIDocumentPickerViewController` — no storage permission | Android MIME filtering unreliable for `.zip`; validate magic bytes yourself. Older publish date = feature-complete |
| `pdf` | 3.13.0 (2026-06-16) | nfet.net ✓ | Flock book + medicine record PDFs. Pure Dart, no `http` in its deps | Base-14 fonts are Latin-1 and **throw** on curly quotes/ellipses — always embed a TTF. `save()` materialises the whole doc — isolate it |
| `archive` | 4.0.9 (2026-02-17) | loki3d.com ✓ | ZIP backup. Deps are only `path` + `posix`. Already transitive via `pdf` | Streaming *encode* unverified — `ZipFileEncoder` exists in `archive_io`; confirm before designing media-in-ZIP |
| `record` | 7.1.1 (2026-06-29) | cow-level.ovh ✓ | Voice notes (spec §7.2). Merges exactly `RECORD_AUDIO` | Class is `AudioRecorder` (not `Record`). Use `aacLc`/`.m4a`, **never opus** (container differs per platform → broken cross-platform restore). Requires Flutter ≥3.44 |
| `in_app_purchase` | 3.3.0 (2026-06-03) | flutter.dev ✓ | One-time unlock (spec §14). Binder IPC / XPC, not an app-process network path | `in_app_purchase_storekit` **must be ≥ 0.4.8** (0.4.3-era regression reported StoreKit2 purchases as `restored` and left them unfinished). Play Billing 9 mandatory 31 Aug 2027 |
| `device_info_plus` | 13.2.0 (2026-06-26) | fluttercommunity.dev ✓ | Model + OS version in the diagnostics header. No `http` in its deps | Do **not** request the iOS 16+ device-name entitlement (user-identifying, spec §4.5) |
| `logging` | 1.3.0 (2024-10-17) | dart.dev ✓ | Named loggers + a `LogRecord` stream wired to a local rolling file | Crash-path writes must bypass the stream (`writeAsStringSync(flush: true)`) |
| **`flutter_riverpod`** | **2.6.1 — pinned exactly** (2024-10-22) | dash-overflow.net ✓ | Context-free DI, `ProviderScope` test overrides, `.select`, `autoDispose`; 4 pure-Dart transitive deps | **Frozen: no bug fixes.** 3.x is not installable alongside `drift_dev` ≥2.34.1. Do not use `StateProvider` or `AsyncValue.valueOrNull`, so a future 4.0 migration is near-free |

**Legitimate fallback, if Riverpod is ever abandoned:** `provider` 6.1.5+1 (2025-08-19, dash-overflow.net ✓) — Flutter's official DI recommendation, adds only `nested`, resolves cleanly. Costs ~16 lines of `StreamSubscription` lifecycle per screen (~150–200 lines across 12 screens), and `BuildContext`-scoped DI is awkward for export and notification code.

**Required but NOT in the c1 audit — must be audited before it is added:** `flutter_timezone` (or an equivalent) is needed to supply the device's IANA name to `tz.setLocalLocation`. It appears only in note 06 and no critic verified it. **Audit it by c1's method (pub.dev API, publisher, transitive graph, merged manifest) and record the verified version here before adding it.** Do not copy note 06's version number into a pubspec.

### 5.2 Dev dependencies

| Package | Version | Why | Cost |
|---|---|---|---|
| `drift_dev` | 2.34.5 (2026-07-22) | `make-migrations`, `schema steps`, `SchemaVerifier`, generated migration tests | **90/160 pub points** — platform support 0/20, up-to-date dependencies 10/40 (*"`analyzer` constraint `^13.0.0` incompatible with latest `14.1.0`"*), no example. Disclosed for honesty; it does not change the adopt verdict. Import `api/migrations_native.dart`, not the deprecated `api/migrations.dart` |
| `build_runner` | **`">=2.15.0 <2.15.2"`** | Required by drift codegen | `^2.15.2` **does not resolve**. `--workspace` is explicitly experimental |
| `flutter_lints` | 6.0.0 (2025-05-27) | Baseline lint set | Sets **no** analyzer language modes — pair with an explicit `analyzer: language:` block (decision #109) |
| `mocktail` | 1.0.5 (2026-04-10) | Interaction-ordering / non-invocation assertions only. No codegen | Overuse risk — hand-written fakes beat it for the six gateways |
| `accessibility_tools` | 2.8.0 (2025-11-13) | Debug-only semantic-label / tap-area / large-font checker. Deps: `collection`, `flutter`, `flutter_test` | Its 48×48 default is below the spec's 60×60 — complements, does not replace, the house assertion. **It is a widget that wraps the app tree, so `lib/` imports it** — wire it behind `kDebugMode`, add it to the allowlist, and note it explicitly |
| ~~`glados`~~ | ~~1.1.7 (2023-12-04)~~ | ~~Property-based testing with shrinking for pure value round-trips~~ | **STRUCK 2026-08-01 — it does not resolve, at any version.** `glados` depends on `package:test`, which caps `analyzer <13.0.0` and pins a `test_api` other than the `0.7.11` `flutter_test` pins exactly. `flutter pub get` reports *"glados is incompatible with drift_dev 2.34.5"* for `glados: any`, so this is the whole package and not one version of it — the same failure decision #4 already records for `test` as a direct dependency. `12 §10.6` and `REFERENCES` D5 both pre-authorised the outcome in writing: **if `pub get` reddens, the property layer is deleted, not the pin.** Property layer 1 is deleted; layer 2, the hand-rolled seeded `FlockGenerator`, is unaffected and was never a package. [N00-T03, the first run of decision #5] |
| `golden_screenshot` | 11.0.1 (2026-03-20) | Store screenshots from golden tests | Belongs in `tool/`, not `test/`. 26 likes |

**Documented alternative:** `very_good_analysis` 10.3.0 (2026-06-18) — turns on `strict-casts` + `strict-inference` + `strict-raw-types` (~215 rules), zero dependencies. Noisy day one; disable the package-publishing and taste rules. Rejected as the headline in favour of `flutter_lints` + an explicit strict block, which gets 90% of the value without the noise for a solo developer.

### 5.3 Rejected — with the reason and the alternative

| Package | Version | Why avoided | Alternative |
|---|---|---|---|
| `flutter_riverpod` / `riverpod` | 3.4.1 | Runtime `test`/`flutter_test` dep forces `test` 1.31.0 → `analyzer <13` → cannot coexist with `drift_dev` ≥2.34.1 | `flutter_riverpod` 2.6.1 pinned |
| `riverpod_generator` | 4.0.6 | **Unresolvable in any project**: `analyzer ^13.0.0` + `riverpod_analyzer_utils 1.0.0-dev.10` (`analyzer ^12.0.0`) | Hand-written providers (~1 line each) |
| `riverpod_lint` | 3.1.6 | Same self-contradiction. **No verified publisher tag at all** | None; revisit after `riverpod_analyzer_utils` gets an analyzer-13 release |
| `riverpod_annotation` / `hooks_riverpod` | 4.0.5 / 3.4.1 | Pins `riverpod: 3.4.1` exactly / second mental model + the same runtime dep | — |
| `import_lint` | 2.0.0 | `analyzer ^12.1.0` conflicts with `drift_dev`'s `^13.0.0` — unresolvable on this stack | `tool/check_policy.dart` (zero deps) |
| `custom_lint` | 0.8.1 | `analyzer ^8.0.0` (six majors behind); **upstream repo archived 2026-03-24**, 51 open issues. Publisher is **invertase.io**, not dash-overflow.net | `tool/check_policy.dart` |
| `dart_code_metrics` | 5.7.6 | **`isDiscontinued: true`**, commercial relicense | Dependency-free scripts |
| `freezed` | 3.2.5 | `analyzer >=9.0.0 <11.0.0` — conflicts with **both** `drift_dev` and `build_runner`; unusable whatever build_runner is pinned to | drift rows + hand-written `@immutable` / `sealed` classes |
| `go_router` | 17.3.0 | Core value is URL routing; no web, no deep links. Three majors in ~24 months. Open restoration bugs flutter#117683, #174935 | `Navigator` + typed push helpers |
| `get_it` / `melos` | 9.2.1 / 8.2.2 | Second DI system / nothing to orchestrate at 1 package (melos 8 delegates to pub workspaces anyway) | Riverpod / a 4-target Makefile |
| `test` (direct) | 1.31.0 max | Caps `analyzer <13.0.0` → breaks `drift_dev`. Does **not** arrive via `flutter_test` | `flutter_test` only |
| `google_fonts` | 8.2.0 | `http ^1.0.0`; HTTP fetching at runtime is the default | Bundle the TTF; CI grep for `GoogleFonts` |
| `printing` | 5.15.0 | `http >=0.13.0 <2.0.0`; `PdfGoogleFonts`/`networkImage` are one-line footguns | `pdf` + `share_plus` alone |
| `google_mlkit_text_recognition` | 0.16.0 | Play Services transitive deps contribute INTERNET/ACCESS_NETWORK_STATE; ~38 MB per script on iOS | Cut OCR from v1 |
| `speech_to_text` | 7.4.0 (2026-05-19) | `SpeechListenOptions.onDevice` defaults false and silently falls back to network recognition; no API reports on-device availability | Defer to v1.1; ship the voice note |
| `sqlite3_flutter_libs` / `sqlcipher_flutter_libs` | 0.6.0+eol / 0.7.0+eol | No-op EOL shims (**not** flagged discontinued on pub.dev). Arrive transitively via `drift_flutter` anyway | `package:sqlite3` 3.x |
| `sqflite` / `sqlite_async` | 2.4.3 / 0.14.4 | OS SQLite makes STRICT/FTS5 a per-device lottery / no generated schema snapshots or `SchemaVerifier` | drift |
| `objectbox` / `isar` / `hive` / `hive_ce` / `realm` / `sembast` | 5.3.2 / 3.1.0+1 / 2.2.3 / 2.19.3 / 20.2.0 / 3.8.9+1 | Proprietary format, prerelease-only successors, deprecated upstream, or wrong data model | drift |
| `flutter_secure_storage` | 10.3.1 | Only needed for a DB encryption key; documents the Android-backup `InvalidKeyException` that makes a restore permanently unreadable | No encryption at rest |
| `purchases_flutter` / `flutter_inapp_purchase` | 10.4.3 / 9.6.1 | Mandatory third-party network path + API key / single-maintainer bus factor on a decade-lived dependency | `in_app_purchase` |
| `file_picker` | 11.0.2 | Heavier transitive deps; cloud-picking undercuts the positioning; 12.0.0-beta.7 signals another break | `file_selector` |
| `permission_handler` | 12.0.3 | Every permission needed has a first-party request API; the CocoaPods `PERMISSION_*` macro block is an App Store rejection risk | Per-plugin APIs |
| `camera` | 0.12.0+2 | Merges `CAMERA` + `RECORD_AUDIO`; manual lifecycle handling | `image_picker` system camera |
| `csv` | 8.0.0 | Unverified uploader + fresh breaking rewrite, for ~50 lines of RFC 4180 you need byte control over | Hand-rolled encoder |
| `golden_toolkit` / `alchemist` | 0.15.0 / 0.14.0 | **`isDiscontinued: true`** / CI text-blocking replaces text with coloured squares, destroying the legibility property the goldens exist to prove | Local `LocalFileComparator` harness |
| `patrol` | 4.8.0 | Requires `patrol_cli`, Gradle + Xcode test targets; `flutter test` will not run its tests | Manual check of 4 native surfaces |
| `checks` | 0.3.1 | *"still experimental. For production use cases, please use `package:test` and `package:matcher`."* | `matcher` |
| `spot` / `flutter_glados` / `mockito` | 0.18.0 / 1.1.18 / 5.7.0 | Debuggability not correctness; 1 like / 55 downloads; codegen for 3–6-method interfaces | `mocktail` / hand fakes |
| `sqlite3_test` | 0.2.0 | Only needed if SQL-side time is used, which is banned; cannot support WAL | Ban SQL-side time |
| `flutter_native_splash` | 2.4.8 | Maintainer publicly seeking a new owner (issue #821, **open since 2026-03-06**); rewrites 3 native files as a second source of truth | ~25 lines of hand-written XML/plist |
| `screen_brightness` | 2.1.11 | Unverified uploader; changes a system property the user did not ask for | Cap luminance in the red/amber palette tokens |
| `slang` / `easy_localization` / `intl_translation` | 4.18.0 / 3.0.8 / 0.22.0 | SDK-bundled `gen-l10n` beats a third-party tool for a solo dev; `easy_localization` has an HTTP loader extension point and an unverified uploader | `gen-l10n` |
| `flutter_screenutil` | 5.9.3 | Fights OS text scale; its escape hatch (`textScaleFactor: 1.0`) breaks Apple's Larger Text criterion | `MediaQuery.textScalerOf` |
| `disk_space_plus` / `open_filex` / `ulid` | 0.2.6 / 4.7.0 / 2.0.1 | Unverified uploader + stale / share sheet already offers "Open in…" / community spec, stale, less legible in a CSV | ~20-line channel / `share_plus` / `uuid` v7 |
| `fixed` / `decimal` | 6.1.1 / 3.2.5 | `BigInt` allocations for a problem integer grams solves exactly | `int` canonical units |
| `signals` / `flutter_bloc` | 7.1.0 / 9.1.1 | Elegance / discipline this app's small state surface does not need | Riverpod 2.6.1 |
| `connectivity_plus` / `workmanager` / `battery_plus` | — | The offline-first design pattern's packages. Banned network/sync machinery here | Nothing |
| `fl_chart` / `syncfusion_flutter_charts` / `graphic` | — | Never weighed in the research at all; one static chart does not justify a package that must pass the offline allowlist | Hand-rolled `CustomPainter` |

---

## 6. Corrections applied — do not re-litigate

These are the places where a critic overturned a researcher. Each has been folded into §2; they are listed so nobody re-reads a raw note and reinstates the error.

| Overturned claim | Where it appears | The correction |
|---|---|---|
| "Adopt `flutter_riverpod` 3.4.1" | 01 §7.2, 04 §5.1 | 2.6.1 exact. Reproduced solver failure twice, independently. |
| `ProviderScope(retry: …)` is "mandatory, non-negotiable" | 01 §7.2, §8.4, and c4's own sketch | **Compile error on 2.6.1**, and on 2.6.1 there is no auto-retry to disable. Delete the line, the pitfall and the CI grep — do not port them. |
| Note 02 §4.4 "2.6.1 spelling (also valid in 3.x)" | 02 §4.4 | It is Riverpod-3 code. Two compile errors on 2.6.1. The doc's blanket "every snippet analyzes clean" claim is false and must be deleted or scoped per version. |
| `build_runner: ^2.15.2` | 01, 03, 07, 08 | Does not resolve. `">=2.15.0 <2.15.2"`. |
| `test: any` in dev_dependencies; `test 1.31.2 · adopt`; "comes in transitively via `flutter_test`" | 03 §2.6, 04, 07 | `flutter_test` does not depend on `package:test`. Remove all three. |
| `import_lint` "adopt-with-care" | 04 | Unresolvable alongside `drift_dev`. Verdict: avoid. |
| `sqlite3_flutter_libs` is "DISCONTINUED" | 03, 07, 08, 09 | pub.dev's `isDiscontinued` is **false**. A CI check keyed on that flag will not fire. It is an `+eol` build tag plus a caution box. |
| "flutter_local_notifications has no network dependency in the graph"; "wakelock_plus deps… none network"; "`timezone` — no network" | 06, 09 | **False.** `http 1.6.0` sits on four regular edges. Delete this phrasing. |
| `store_date_time_values_as_text: true` | 03 (marked "High", "irreversible") | Overturned by 09's kind-modelling argument. Integer instants + text civil dates. |
| Civil-day `DateTime(y, m, d + n)` for withdrawal | 01 §5, 04 §2 | Measured 167 h across spring-forward — one hour short of a 7-day withdrawal, in late March. Absolute hours, ceil to local midnight. |
| "Do not store `clear_date`" | 09 | Store it. Spec §10 lists it; the PDF handed to a vet must match what the app displayed on the day. |
| Four different `main()` implementations | 01, 02, 05, 08 | 08 wins: nothing awaited, everything after the first frame. |
| `driftDatabase(name: 'shed_book')` with the default directory | 01 §6.4 | Application Support. |
| `validateDatabaseSchema(this)` inside a sync `assert` | 03 §3.3 | It is an extension member returning `Future<void>`. `if (kDebugMode) { await db.validateDatabaseSchema(); }`. |
| A drift Dart view using `groupBy` | 01 §10.3 | Undocumented for Dart-defined views, and the cascade returns the wrong object. Use `customSelect` + `readsFrom:`. |
| Tap-target test without `ensureSemantics()` | 05 §6.4 | The gate throws rather than passing. Add the handle. |
| `_AtLeast` overriding `textScaleFactor`, no `==`, constructed in `build()` | 05 §2.5 | Deprecated member the house rule bans; invalidates every MediaQuery dependant on every rebuild. |
| `Clock.fixed` + `pump(Duration)` for elapsed-time widget tests | 04 §2.3 | `Clock.fixed` freezes `now()`; the test silently measures 0 h. |
| Free-tier guard on `FlockRepository.createEwe` | 01 §18, 02 §11.4, 03 open q4 | Policy object + `EntryContext`; `liveEntry` cannot be blocked. |
| `weightBump: 100` for boldText and red-shift | 05 | w800 renders *lighter* with Bold Text on (flutter#139712). Cap at w700. |
| "spec open question 4 is answered: no" | 05 #10 | It is a hardware test. Reopened. |
| `highContrastDarkTheme` set to the same object as `darkTheme` | 05 §2.1 | Dead plumbing while claiming to honour `highContrast`. Ship a genuinely higher-contrast palette plus an in-app toggle. |
| Amber replacing red-shift | 10 | Spec names red-shift twice. Ship both, labelled honestly. |
| `intl 0.20.3 · adopt` | 10 | It resolves to 0.20.2. Declare `intl: any`. |
| `dart tool/check_policy.dart` as the gate's invocation | 01 §3.2/§3.3, CONVENTIONS §1, 13 §1.3/§4.3, `CLAUDE.md`, twelve skills | **`dart tool/check_policy.dart`** — no `run` subcommand. Measured 2026-08-01 in N03-T01: `dart run` performs an implicit `pub get` and executes the package's build hooks, so on a cold hook cache with the network away it fails at a **pub.dev advisories fetch** — which is exactly the failure decision #9/#10 exist to prevent, *"it can fail for reasons that are not violations"*. Without `run` it exits 0 on the same cold, network-blocked tree. |
| `http` sits on **two** regular edges | decision-record §3.4 #1, 08 §9, 12 §10.2, 13 §2.4, CODE-REVIEW-CHECKLIST, `CLAUDE.md`, 00-README, two skills | **Four.** Measured 2026-08-01 with `dart pub deps --json` in N03-T04: `timezone`, `package_info_plus`, `file_selector_platform_interface` and `image_picker_platform_interface` all depend on `http` directly. The count only ever appeared as an argument for why the lockfile rule is unsatisfiable, and four is a stronger argument than two — but a number in a document is a number somebody will check. |
| The gate runs in "< 1 s" | 01 §3.3 | **~2.5 s**, measured on the pinned SDK. The walk is milliseconds; JIT startup is the rest. It is still by far the cheapest step in `check` and its position does not change — but a number in a document is a number somebody will hold it to. |
| `custom_lint` publisher "dash-overflow.net (verified)"; `riverpod_lint` "verified" | 02 | `custom_lint` is **invertase.io**; `riverpod_lint` has **no publisher tag**. A table using "verified publisher" as a trust signal cannot get the publisher wrong. |
| "Adopt them verbatim" (SRUC lambing-ease descriptions) | 09 §1.2 | The cited PDF is image-based; the quoted scale and its licence terms **could not be verified**. Paraphrase; treat the quotation as unverified. |
| `glados 1.1.7 · adopt-with-care`; "verified to resolve against this stack" | c1 §4.2, §5.2 of this file, 12 §10.6, 00-README §3, REFERENCES D5 | **It does not resolve at any version.** `glados` → `package:test` → an `analyzer` and `test_api` pair that cannot coexist with `drift_dev` 2.34.5 and `flutter_test`'s exact `test_api: 0.7.11`. §5.2's row was a table entry nobody had run, which is precisely the failure decision #5 exists to catch, and this is the first time anybody ran it. The property layer is deleted, not the pin — the outcome `12 §10.6` and `REFERENCES` D5 had already written down. |
| Toolchain baseline 3.44.6 | all ten | Current stable on the research date was 3.44.8; the SDK pins are identical, so nothing flips, but the baseline must be stated and re-resolved. |
| Dart version "discrepancy" left open | 08 §0.2 | Resolved: **Dart 3.12.2**. Record the answer, do not carry the discrepancy forward. |

---

## 7. Open questions only the owner can answer

### 7.0 SETTLED BY THE OWNER

These were put to the owner and answered. They are **no longer open**, and the engineering doc set is written against them. Anything in this document or in `docs/research/raw/` that contradicts them is superseded. Each has a matching struck item in §7.1 carrying the same date — the two halves are asserted to agree by `test/policy/dependency_rulings_test.dart`.

| # | Question | **Ruling** | What it binds |
|---|---|---|---|
| 5 + 6 | Do tag OCR and voice tag entry ship in v1? | **Both cut from v1.** The voice *note* still ships as pure local recording via `record`. | The offline gate stays mechanically provable: no ML Kit, no `speech_to_text`, no Play Services in the graph. `docs/08-platform-integration.md` documents both as v2 candidates with the reason, so the decision is not re-litigated by a future contributor. |
| 7 | Tag uniqueness | **Unique among ACTIVE animals only** — a partial unique index, not a global one. | The schema freeze. A culled 412 releases the tag; a new 412 is a new row with its own identity and its own history. Create-on-the-fly must therefore match against active animals only, and the ewe card must be able to show "there was an earlier 412". |
| 3 | Region first | **UK / Ireland.** | Ambiguous DST hour is **01:00–01:59** — that is the hour the withdrawal and hours-penned tests must target. Default locale `en_GB`, kg, °C, 24-hour clock, `dd/MM/yyyy`, week starts Monday. The default lambing-percentage convention follows AHDB (lambs born **alive**) and the definition remains user-configurable per spec §7.8. |
| 8 | Free-tier shape | **Season-primary, ewe cap secondary.** | The free tier covers one full season; the gate lands where §7.7 says the value is — opening last year's history in season two. The ewe cap is a calm secondary gate. Neither may surface mid-entry, and neither may surface at all between 22:00 and 06:00. |
| 16 | Is a printable PDF required to print *from inside the app*? — **ruled 2026-08-01** | **No. There is no in-app print dialog.** Delivery is the share sheet, and printing happens from the OS Print action the share sheet already offers on both platforms. | `printing` 5.15.0 stays rejected in §5.3, so the `http >=0.13.0 <2.0.0` edge it declares never enters the graph on our account and G3's `PdfGoogleFonts` / `networkImage` greps stay **blocking** rather than becoming advisory. `pdf` alone remains the PDF producer (#83). The Export screen says the PDF goes to the share sheet and printing happens from there; it never prints the word *printable* and leaves the shepherd hunting for a button. Tier 2 of the §3.1 offline claim — *no dependency attempts a network call from our process* — does not have to be re-argued, which it would have if a package whose whole job is talking to a print service were admitted. |
| 18 | Voice note cap: 60 s or 120 s? — **ruled 2026-08-01** | **60 seconds.** One constant `kVoiceNoteMaxSeconds` in `lib/data/media_limits.dart`, written in N15 and referenced everywhere including the storage-budget test. | AAC-LC mono at 32 kbps is ~240 KB at 60 s against ~480 KB at 120 s, on a typical-season media figure of ~300 MB (`04 §4.4`). 60 s is chosen as the **recoverable** direction, not the cheap one: raising a cap orphans nothing, whereas lowering one makes recordings that already exist unreproducible. If the field night (question 1) shows a shepherd talking for ninety seconds about a malpresentation, the amendment rule applies and the cost is one constant and one row — which is exactly why the cap was ruled downward first. |

| 10 | Is the target market ever a dairy flock? — **ruled 2026-08-01** | **Ship `WithdrawalTarget.milk` in the v1 schema.** The v1 UI may never write one. | `treatment_withdrawals` keeps `CHECK (target IN ('meat','milk'))` and its `{treatment, target}` unique key, so 0..n rows per treatment already express a second target at no cost. `WithdrawalMilkings` does **not** exist in v1 and nothing converts milkings to days. `09 §10` row 12 already says the `milk_*` columns ship in `treatments.csv` regardless. Ruling *no* would have meant deleting `milk` from the enum and the CHECK and re-admitting it later as a migration plus a re-read of every withdrawal surface — the asymmetry is the whole argument. |
| 11 | Where does temperature appear at all? — **ruled 2026-08-01** | **Nowhere. Drop the setting.** No v1 table stores a temperature. | `app_settings.temperature_unit` and its `CHECK (temperature_unit IN ('c','f'))` do not exist in v1; nor does the Settings °C/°F row, nor `temperatureUnitProvider`. `MilliCelsius` **still ships** (`05 §5.2`) and costs nothing — it is not deleted, because the measured reason it exists is that storing temperature at 0.1 °C silently rewrites 89 of 201 °F entries. This is the only window in the project where dropping a column is free: migrations are forward-only and never destructive (#37), so before the first snapshot the column can simply never exist, and after it the column ships forever, unread. An unused setting is a 3am tax. |
| 13 | Does a lamb kept as a breeding ewe become a `Ewe` row? — **ruled 2026-08-01** | **Yes.** `lambs.became_ewe`, nullable, in the v1 schema. | A nullable FK to `ewes(id)` with `onDelete: KeyAction.setNull`, plus a hand-written `@TableIndex(name: 'idx_lamb_became_ewe', columns: {#becameEwe})` — SQLite creates no child-key index automatically (#31). It re-opens the cross-table tag rule at `03 §6`, whose sentence *"a lamb tagged 412 and an active ewe tagged 412 can coexist… v1 has no lamb→ewe promotion"* is only true while the answer is no: `03 §6` is amended in the same commit and **no cross-table trigger is added to paper over it**, by that section's own instruction. The partial unique index on active tags is unchanged — promotion writes a `ewes` row that takes its own tag through the same create-on-the-fly path every other ewe does. |
| 19 | Does the whole remaining backlog ship as one release? — **ruled 2026-08-03** | **No. Two releases: `v1.0.0` before 1 February 2027, `v1.1.0` on 1 June 2027.** Twenty-six of ninety remaining tasks move; nothing is cut. Long form in §7.0c (**P15**); scope in [`docs/RELEASE-SCOPE.md`](../RELEASE-SCOPE.md). | `13 §11`'s freeze is the binding constraint, not effort: 1 Feb – 30 Apr is the only time of year the app is used, so a release that misses 31 January 2027 slips by a **year**. The freeze blocks releases and not work, so `v1.1.0` is built Feb–May 2027 and costs no calendar. Three things `v1.0.0` must build for a release it does not contain: the backup format ships **whole** (all 21 tables — `unknown_json` carries an unknown *column* forward, never an unknown *table*); **no notification channel is created**, so #63/#65's *ids frozen at release* freezes nothing; and `android/expected_permissions.txt` is `v1.0.0`'s strictly smaller set with the three `v1.1.0` adds named in the same file, because G1 asserts set equality and going red must be answerable from a file. Every epic header carries a `**Ships in**` row and `tool/validate_epics.py` fails while one disagrees with the scope table. |
| 15 | Lambing ease: 5 points or SRUC's 6? — **ruled 2026-08-01** | **Five**, and point 5 covers elective caesarean. | `lambings.ease` stays `integer().nullable()` with `CHECK (ease IS NULL OR ease BETWEEN 1 AND 5)`; `LambingEase` stays validated 1..5 (R44); the five labels stay `vocab_terms` rows `ease_1`…`ease_5` with ARB defaults; the CSV column stays `lambing_ease_1_5` (`09 §3.1`); `assistedRate`'s *"ease ≥ 2"* numerator and its verbatim `definition` string are unchanged (`05 §6.7`); `ShedChoiceRow`'s *ease 1–5 only* contract from N10-T06 stands. `lambings.ease` is deliberately **not** a vocabulary foreign key, so widening the scale is a migration somebody has to think about, and that friction is the feature. **A blank ease is not "unassisted"** — it means not scored, and `05 §6.7` excludes unscored lambings from both sides of the assisted rate and reports coverage. |

### 7.0c P15 — the two releases. RULED 2026-08-03 (the owner, after N20).

**The question.** N00–N20 are merged and ninety tasks remain across N21–N34. `13 §11` freezes
releases for **1 February – 30 April**, which is the only time of year this app is used, and the open
window on the far side of today is **now until 31 January 2027**. Does the whole backlog ship as one
release, or does the product ship twice?

**The ruling: twice. `v1.0.0` before 1 February 2027; `v1.1.0` on 1 June 2027**, the first day after
the freeze and May's staged-rollout window. Scope, task by task, is
[`docs/RELEASE-SCOPE.md`](../RELEASE-SCOPE.md), which `tool/validate_epics.py` holds against
thirty-five epic headers.

**Why this is a calendar ruling and not a scope preference.** A release that misses 1 February does
not slip by a month; it slips by a **year**, into a season whose shepherds have already chosen a
notebook or a competitor. And the freeze blocks *releases*, not *work* — so February to May 2027 is
four months of development that were going to be spent anyway, and `v1.1.0` costs no calendar at all
as long as its regression armour ships first (§5.5 of the scope document: the goldens, `goldens.yml`,
the four journeys and the geometric tap-target gate are all `v1.0.0`).

**What moves:** reminders (N24, N25), Season Summary (N28), the two PDFs (N21-T04/T05), note search
(N26-T05/T06), terminology editing and the two deletes (N29-T03/T06). **Twenty-six tasks of ninety.**

**What does not, and the test that sorts them.** *If this is missing on the night of 3 March 2027,
what happens?* If the answer is *a record is lost or cannot be got off the phone*, or *the app cannot
be sold or installed*, it is `v1.0.0` — there is no server, no sync, no remote fix, and that night the
app is frozen. Export, backup, restore, the unlock, the permission gates, signing and the ship gates
are all in that class. **A deferral with no answer to *"what do they do instead"* is a cut, and would
belong in spec §13 rather than here.**

**Nothing is cut.** Every deferred item ships. Three consequences are load-bearing and are recorded
where they will be read rather than only here:

- **The backup format ships whole in `v1.0.0` — all 21 tables, `reminders` among them, empty.**
  N22-T03's forward-compatibility contract carries an unknown *column* through `unknown_json`; it does
  **not** carry an unknown *table*. Ship the format complete and a `v1.0.0` backup restores into
  `v1.1.0` unchanged, and N23-T07's equality property holds across the boundary.
- **`v1.0.0` creates no notification channel**, so #63/#65's *ids are frozen at release* freezes
  nothing, and `v1.1.0` is the first release that fixes the eight. Deferring is strictly safer than
  shipping here.
- **`v1.0.0`'s permission set is strictly smaller** — no `POST_NOTIFICATIONS`,
  `RECEIVE_BOOT_COMPLETED` or `SCHEDULE_EXACT_ALARM`. N31-T01 writes that set and, in the same file,
  the three lines `v1.1.0` adds and the epic that adds them, because §3.2 is built on G1 going red
  being answerable from a file rather than from memory.

**The words.** An epic ships in **`v1.0.0`** or **`v1.1.0`** — the release tag, and no new vocabulary.
**Never "v1"/"v2" for these two**: spec §7 and §13 already use those for *the product* and for *cut,
maybe never*, so *"reminders are not in v1"* would say the opposite of what is true. `R1`/`R2` are
unusable for the same reason — `CONVENTIONS.md` and `epics/README.md` spend them on rule ids.

### 7.0b P8 — the birth-type chooser. RULED 2026-08-02 (N16-T02a).

**The question.** `07 §6.4` gives Lambing Entry a birth-type chooser — *"Declare birth type — 1 tap,
five big buttons"* — and makes it the sixth tap of the product's headline budget. `12 §10.1`'s
published test spends that tap on `Key('lambing_entry.birth_type.twin')`, and `CONVENTIONS §4.5` uses
the same key as its worked example of the key format. Does the chooser ship?

**The ruling: no. Birth type is DERIVED from the tally strokes and labelled `(COUNTED)`.**

**Why, and it is a safety rule rather than a simplification.** Spec §12.4 forbids silently correcting a
user's entry. A declared birth type and a counted one can disagree — *"twin"* declared, three strokes
recorded — and every resolution of that disagreement is bad: correcting the declaration is §12.4 in the
plainest possible form; correcting the tally deletes an animal; showing a warning asks a shepherd at
03:20 to adjudicate between two numbers the app collected. **Deriving the type removes the
disagreement rather than handling it**, which is what makes §12.4 structural here instead of
procedural — the mechanism hierarchy's *unrepresentable* rather than its *documented*.

The sixth tap survives, and so does the fifteen-second promise: it is now the first tally stroke, on
`lambing_entry.tally.stroke`. `07 §1.3`'s budget stays at **6** — only its composition changes.

**What this binds.**

- `lambings.declared_birth_type` **keeps its column and its writer.** The deferred CHANGE TYPE path
  still sets it, and NULL still means *not declared* (R6) — never `single`. What no longer exists is a
  control that declares one on the five-tap path.
- `countedBirthType(int)` (`lib/domain/birth_type.dart`, N16-T02) is the derivation, and it returns
  **null at zero and at five or more**: zero is NOT RECORDED, and a counted five is not `quintPlus`,
  which means *"more than four, count not declared"*.
- **No key anywhere in the tree may contain `birth_type`.** Held by a tree-walking canary in
  `test/features/lambing_entry_test.dart`, not by review.
- `06 §12`'s `ShedChoiceRow` is **ease 1–5 only**. N10-T06 shipped it that way with a doc comment
  naming P8; the document catches up here.

**What is struck.** `07 §6.4`'s chooser and its tap, `07 §6.3`'s *"the five buttons are unselected"*
row, `12 §10.1`'s sixth tap, and `CONVENTIONS §4.5`'s worked example — **a naming authority that
publishes a key for a control the product does not have is worse than a missing example**, because a
fixer applies it mechanically.

---

### 7.0a P3 — the navigation model. RULED 2026-08-02 (N13-T01).

**The conflict.** `02 §8.1`–§8.3 and §9.1 specify `Navigator` 1.0, a stack at most three pushes deep,
twelve typed push helpers, `PopScope` with `canPop: true` everywhere, Android predictive back, and a
two-minute resume reset to Quick Entry. `indelible.md` §7.17 says *"There is no tab bar, no rail, no
stack, and no back button — pressing `INDEX` and choosing another filter is always one press deeper,
never one press back"*, and its Screen 3 says *"There is no Quick Entry screen, and that is the
design."* The two are not reconcilable by wording.

**The ruling splits, because the disagreement has two halves and they resolve opposite ways.**

| Half | Ruling | Why the other answer was wrong |
|---|---|---|
| **Mechanism** — is navigation a `Navigator` stack at all? | **`02` wins. It is a stack.** | Indelible's *"one press deeper, never one press back"* is a claim about **what the shepherd is told and sees**, not about the widget tree. A push whose only exit is `Routes.popToQuickEntry` satisfies it from the user's side while keeping three things that are otherwise hand-built on every screen: the route name the diagnostics log is allowed to record (#124), `ModalRoute.withName` for the pen-board flow's explicit return, and Android's hardware **and predictive** back. Deleting the stack means owning the Android back button by hand, twelve times, in an app whose users are one-handed at 03:20. That is the higher-risk half and it buys nothing the user can perceive. |
| **Affordance** — does an on-screen back control exist? | **Indelible wins. There is no back chevron anywhere in this app.** | This is not a concession: `02` never *required* a visible back control, only that the platform gesture works. `INDEX` (96 × 64, bottom-left, `indelible.md` §4.5) is the only navigation affordance, and `AppBar`'s automatic leading button is therefore suppressed on every screen. A chevron would teach a shepherd that the app has a history to walk backwards through, which is exactly the model Indelible is refusing. |

**What is struck.** `indelible.md` §7.17's *"no stack"* — the mechanism claim only — with this reason.
Its *"no back button"* stands and is now enforceable. Screen 3's *"There is no Quick Entry screen"* is
struck too: `07 §5` makes Quick Entry the root route and N13-T05 builds `QuickEntryScreen`, and
`07-screens.md` outranks `indelible.md` on which screens exist. What Screen 3 was describing — one
scrolling ruled page under different filters, `INDEX` bottom-left, the slab bottom-right — is a layout
claim and it survives untouched.

**What this binds.** `lib/routing/routes.dart` is the only place a route is constructed;
`Routes.navigatorKey` is the one `GlobalKey`; `canPop` is `true` on every screen (the single exception
is the season-deletion flow, #69); `onPopInvokedWithResult`, never the deprecated spelling; no
`restorationScopeId` and no `Restorable*` (#24); and **no screen renders a back affordance** —
`automaticallyImplyLeading: false` wherever an `AppBar` appears, asserted per screen from N13-T05
onward.

---

Questions 1, 2, 4, 9, 12, 14 and 17 below remain genuinely open. Items 3, 5, 6, 7 and 8 were settled on 2026-07-27; items 16 and 18 on 2026-08-01, before `pubspec.yaml` closed; items 10, 11, 13 and 15 on 2026-08-01, before the schema freeze at N07-T08.

---

### 7.1 Still open

Ranked by what they block.

**How to read this list.** Every item carries exactly one **shape tag**, and the shape says which
artefact the question expires into rather than how important it is:

| Shape | Expires when | Answered by |
|---|---|---|
| `dependency-shaped` | `pubspec.yaml` closes (N00-T03) | a package enters the graph or does not |
| `schema-shaped` | the first snapshot is committed (N07-T08) | a table, a column or a constraint |
| `calendar-shaped` | somebody else's diary fills up | a booking, recorded in `docs/calendar.md` |
| `product-shaped` | nothing — it can be answered in week two without rework | the owner, at leisure |

A ruled item is **struck, never deleted**, and carries a line reading
`RULED <YYYY-MM-DD> — <one sentence>` beneath it. §6 exists precisely so a re-read of a raw research
note cannot reinstate an overturned claim; deleting a ruled item is how the same question comes back
from the notes in three months. `test/policy/dependency_rulings_test.dart` and
`test/policy/schema_shaped_rulings_test.dart` parse exactly these two markers.

1. **Does a night in a real lambing shed happen before the Quick Entry screen is written?** (spec §17.1) The entry flow *is* the product and cannot be designed correctly from forum posts. It also closes questions 2, 12 and 18 below. **This is the highest-value unresolved item in the project.** `calendar-shaped` — ledger row `field_night`.
2. **Ziplock-bag capacitance** (spec §17.4). Does the target hardware register taps through a freezer bag? If not, the entire interaction model has to be rethought around volume-button shortcuts, and decisions #100, #101 and #102 all change. This is a hardware test, not a desk decision. `calendar-shaped` — ledger row `ziplock_capacitance`.
3. ~~**Which region first?** (spec §17.3) UK/Ireland's ambiguous DST hour is 01:00–01:59; continental Europe's is 02:00–02:59. It also decides the default lambing-percentage convention (AHDB counts lambs born *alive*; Sheep Ireland counts dead lambs in "born").~~ `product-shaped`
   **RULED 2026-07-27 — UK/Ireland first, so the ambiguous hour every time-shaped test targets is 01:00–01:59; see the §7.0 SETTLED row for what it binds.**
4. **Exact price and territories.** €10–15 is a range. Enrol in the Apple Small Business Program (15%) **before the first sale**. Google restructured fees on 30 June 2026 and the secondary sources disagree in detail — confirm the exact one-time-product rate for IE/UK/EEA inside Play Console before committing to a price. `calendar-shaped` — ledger rows `price_and_territories` and `apple_sbp_enrolment`.
5. ~~**Does tag OCR ship at all?** Recommendation: **no, not in v1**, on offline-purity, size and platform-symmetry grounds. Owner call, because it is a named spec feature.~~ `dependency-shaped`
   **RULED 2026-07-27 — Cut from v1, so `google_mlkit_text_recognition` never enters the graph and the Play Services transitive edges that would contribute INTERNET never have to be argued about.**
6. ~~**Does voice tag entry ship at all?** Recommendation: **no, not in v1** — the recognizer is another process whose network access we cannot constrain. The voice *note* ships instead. Owner call, same reason.~~ `dependency-shaped`
   **RULED 2026-07-27 — Cut from v1, so `speech_to_text` never enters the graph; the voice note ships through `record` instead, which is a pure local recording our manifest can constrain.**
7. ~~**Tag uniqueness: globally unique forever, or unique among active animals?** Real flocks reuse tag numbers after culls. This changes the unique index to a partial one and changes the create-on-the-fly flow. **Must be settled before the schema is frozen.** Needs a shepherd, not a developer.~~ `schema-shaped`
   **RULED 2026-07-27 — Unique among active animals only, which makes the index on `ewes.tag` a partial unique index rather than a global one.**
8. ~~**Cap shape: 15 ewes, one season, or both?** Recommendation: season-primary (the calmest possible gate, landing exactly where §7.7 says the value is), with the ewe cap as a secondary calm-UI gate. The spec offers both and calls it open; decide it once.~~ `product-shaped`
   **RULED 2026-07-27 — Season-primary with the ewe cap secondary; neither surfaces mid-entry and neither surfaces at all between 22:00 and 06:00.**
9. **Does the app replace the paper record entirely, or sit alongside it for the first season?** (spec §17.2) This changes how hard the export has to work and whether records-only JSON backup is acceptable. `product-shaped` — **bound but not answered by P15**: `v1.0.0` ships the three CSV shapes and no flock-book PDF, so for the 2027 season the honest answer is *alongside*, and the Export screen and the store listing say so in those words.
10. ~~**Is the target market ever a dairy flock?** If yes, `WithdrawalTarget.milk` (and possibly milkings) must be in the **v1 schema** even if not in the v1 UI. Shipping the sealed type now is free; retrofitting is a migration.~~ `schema-shaped`
    **RULED 2026-08-01 — Ship the target. `treatment_withdrawals.target` keeps its `CHECK (target IN ('meat','milk'))` and `enum WithdrawalTarget { meat, milk }` keeps both members, because 0..n rows per treatment already express it and admitting `milk` later is a migration plus a re-read of every withdrawal surface; `WithdrawalMilkings` does not exist in v1 and nothing converts milkings to days.**
11. ~~**Where does temperature appear at all?** Spec §7.10 has a °C/°F setting but §10's data model has no temperature field. Either the setting is vestigial (drop it — an unused setting is a 3am tax) or a lamb/ewe body-temperature field is missing. Decide before schema v1.~~ `schema-shaped`
    **RULED 2026-08-01 — Drop it. `app_settings.temperature_unit` does not exist in v1, no v1 table stores a temperature, and `temperatureUnitProvider` and the Settings °C/°F row go with it; this is the only window in the project where removing a column is free, because migrations are forward-only and never destructive, so after the first snapshot it would be a column that ships forever unread.**
12. **Lamb-scale resolution and the plausible weight band.** Grams as canonical is right either way, but the *input step* (0.1 kg? 0.05 kg? 50 g?) needs twenty minutes with a real shepherd. `product-shaped`
13. ~~**Does a lamb that is later kept as a breeding ewe become a `Ewe` row?** If yes, `Lambs` needs a nullable `became_ewe` FK and the retention story gets much better. A v1 schema decision with v3 consequences.~~ `schema-shaped`
    **RULED 2026-08-01 — Yes. `lambs.became_ewe` is a nullable FK to `ewes(id)` with `onDelete: setNull` and a hand-written index `idx_lamb_became_ewe`, because SQLite creates no child-key index automatically (decision #31); it is free before the freeze and it is what makes *"her dam was 412"* answerable in season three, which is the recall the product is sold on.**
14. **Does the developer account already exist, and is it a personal account created after 13 Nov 2023?** If yes, the **12-tester / 14-day closed test** is on the critical path and must be scheduled now, not at the end. **And: who are the twelve testers?** That recruitment doubles as the answer to question 1. `calendar-shaped` — ledger rows `developer_accounts` and `twelve_testers`.
15. ~~**Lambing ease: the spec's 5 points or SRUC's 6 (with elective caesarean)?** Recommendation: stay at 5 — five big buttons is the 3am-correct answer — and document that 5 covers elective. Decide before any data exists.~~ `schema-shaped`
    **RULED 2026-08-01 — Five. `lambings.ease` keeps `CHECK (ease IS NULL OR ease BETWEEN 1 AND 5)` and `LambingEase` keeps its 1..5 validation, with point 5 documented as covering elective caesarean; five big buttons is the 3am-correct answer and the column is deliberately not a vocabulary foreign key, so widening the scale stays a migration somebody has to think about.**
16. ~~**Is a printable PDF required to print *from inside the app*?** Only that justifies re-admitting `printing` and its `http` dependency behind a CI gate.~~ `dependency-shaped`
    **RULED 2026-08-01 — No in-app print dialog, so `printing` 5.15.0 stays out of the graph and delivery remains share sheet to the OS Print action; re-admitting it would hand every future contributor `PdfGoogleFonts` and `networkImage` as one-line network paths on the platform with no permission gate to stop them.**
17. **Does the free tier cap reminders too?** 15 ewes fits inside the 56-slot iOS budget comfortably; 400 does not. The cap interacts with the notification architecture. `product-shaped` — **off `v1.0.0`'s critical path as of P15**: reminders ship in `v1.1.0`, so this expires with N24 rather than before the first release. It is not ruled and is not deleted.
18. ~~**Voice note cap: 60 s or 120 s?**~~ Drives the storage budget. `dependency-shaped`
    **RULED 2026-08-01 — 60 seconds, because it is the lower storage figure (~240 KB against ~480 KB per note at AAC-LC mono 32 kbps) and the recoverable direction: raising a cap orphans nothing, lowering one makes existing recordings unreproducible.**
19. ~~**Does the whole remaining backlog ship as one release?** Ninety tasks remain across N21–N34 and `13 §11` freezes releases for 1 February – 30 April, which is the only time of year the app is used. The open window closes on 31 January 2027 and there is no second one until June.~~ `product-shaped` — the P15 half is §7.0c.
    **RULED 2026-08-03 — No. Two releases: `v1.0.0` before 1 February 2027 and `v1.1.0` on 1 June 2027, because a release that misses the season slips by a year rather than by a month, while the freeze blocks releases and not work — so the four months it defers into were going to be spent building anyway. Twenty-six of ninety tasks move (reminders, Season Summary, the two PDFs, note search, terminology editing, the two deletes); nothing is cut; the scope is `docs/RELEASE-SCOPE.md` and `tool/validate_epics.py` holds it against thirty-five epic headers.**

---

## 8. The engineering doc set to write

Thirteen numbered documents, each covering exactly one concern, plus an index, a review checklist (spec §12 explicitly asks for one and no note produces it), and a bibliography.

| File | Scope (one line) |
|---|---|
| `docs/00-README.md` | Index of the doc set, the pinned toolchain, repo conventions (naming, `snake_case.dart`, `<feature>_screen`/`<feature>_controller`, providers `<name>Provider`, widget keys `dotted.lower_snake`), git hygiene (what is committed vs ignored), and the one-page "how a feature gets added" recipe. |
| `docs/01-architecture.md` | Layers, the folder tree, the eight dependency rules and the single `tool/check_policy.dart` gate, MVVM-with-no-remote-source, the repository write path, `WriteOutcome`/`ShedFailure`, the global error net, and the ~20-line `main()` every other doc links to instead of re-printing. |
| `docs/02-state-di-navigation.md` | `flutter_riverpod` 2.6.1 idioms and the explicit ban list of Riverpod-3 APIs, provider shapes and the DI graph, controller conventions, the double-tap `WriteController`, `Navigator` + typed route helpers, and why there is no state restoration. |
| `docs/03-data-model-and-schema.md` | The 18-odd tables with their `STRICT`/FK/`CHECK` conventions, the dual-key ID strategy, fostering and pen-occupancy history, `CareEvents` and `EweObservations`, the first-run seed, and the tag-uniqueness decision once the owner answers. |
| `docs/04-migrations-media-backup-restore.md` | Forward-only migration rules, `make-migrations`, the full from→to test matrix and the no-diff CI check, the media filesystem layout and relative-path rule, orphan sweeps, `VACUUM INTO` as a diagnostics snapshot, and the atomic replace-everything restore. |
| `docs/05-domain-correctness.md` | `Instant`/`LocalDate`, the clock and the SQL-time ban, the withdrawal sealed type and clear-date algorithm, `RecordedTime`, canonical grams and milli-°C, `StatResult` and every statistic's edge cases, terminology, and the five safety rules as structural mechanisms. |
| `docs/06-design-system.md` | The three hand-authored palettes and the high-contrast slot, two-tier tokens, typography and the w700 cap, tap targets and the gesture ban, the custom keypad, the no-white-flash recipe at four layers, feedback channels, and the pen-board glanceability rules. |
| `docs/07-screens.md` | A brief per screen: purpose, the one query that feeds it, every state including empty, the actions and their tap costs, which §12 disclosures appear — plus the undo-per-verb table, the export-prompt banner, and the reminder/OS reconciliation rule. |
| `docs/08-platform-integration.md` | Notifications (the reconcile architecture, channels, exact alarms, reboot, DST), photo capture and compression, audio recording, the share sheet, file import, wakelock, and the per-plugin permission-request policy. |
| `docs/09-export-formats.md` | The RFC 4180 CSV writer and its three shapes, the PDF build (embedded TTF, isolate, splitting, memory), the JSON backup envelope and its forward-compatibility rules, and the disclaimer footers required by §12.1 and §12.3. |
| `docs/10-accessibility-and-i18n.md` | The platform flag truth table, semantics for the pen board / keypad / chart, headings, text scaling, colour-never-alone, Apple's Nutrition Label criteria as the ship gate, and the gen-l10n/ARB setup with the terminology-placeholder rule. |
| `docs/11-monetization-and-store.md` | The IAP model, the entitlement row and its three rules, the purchase and restore flows, the free-tier policy object and `EntryContext`, the upgrade affordance constraints, and both stores' privacy declarations and required artefacts. |
| `docs/12-testing.md` | The five test tiers, time in tests, the in-memory drift harness, hand-written fakes, the 216-cell overflow matrix, the accessibility gates, the ~8-golden policy and how to re-baseline, the four integration journeys, coverage policy, and the seed/fixture strategy. |
| `docs/13-build-ci-release.md` | The offline gates G0–G5 and the permission set, the CI job matrix and its macOS budget, lints and the analyzer strict block, size and startup budgets with `docs/perf/measurements.md`, versioning and signing, test tracks and the 12-tester requirement, and the seasonal release freeze. |
| `docs/CODE-REVIEW-CHECKLIST.md` | Two sections — "CI already proves this" (so reviewers do not waste attention) and "a human must check this": the five §12 rules restated as questions, plus the 400 ms debounce ceiling, no `combineLatest` over drift streams, no `FittedBox` around user-facing text, no `Tooltip`/`Dismissible`/`Draggable`, no raw colour literals, no `DateTime.now()`, no `?? 0` on a nullable aggregate. Each item cross-links to the note section that argued it. |
| `docs/REFERENCES.md` | Every primary source cited across the doc set, grouped (Flutter/Dart, drift/SQLite, platform docs, store policy, WCAG/Apple accessibility, agricultural and veterinary), each with the fetch date — plus an explicit list of sources that could not be verified (the SRUC TN747 PDF text and licence, the Play Billing 8.0.0 AAR manifest, `ZipFileEncoder` streaming behaviour). |

**Sizing note.** Documents 03 and 05 are the two that most want to merge and must not: schema shape and domain semantics fail differently and are edited by different tasks. Documents 07 and 06 are also deliberately split — the design *system* is stable and the screen *briefs* churn.
