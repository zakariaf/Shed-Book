<?xml version="1.0" encoding="utf-8"?>
<!--
  Identical to values/styles.xml on purpose. The app is dark-only, so there is
  nothing for dark mode to change — and keeping the two files the same is what
  makes a divergence obvious in a diff rather than a surprise on one device.
-->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/shed_surface_base</item>
        <item name="android:statusBarColor">@color/shed_surface_base</item>
        <item name="android:navigationBarColor">@color/shed_surface_base</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/shed_surface_base</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
