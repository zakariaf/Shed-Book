<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- THE TWO REMOVALS, AND NEITHER IS COSMETIC.

         INTERNET is merged by com.google.android.datatransport:transport-backend-cct,
         a compile-scope dependency of Play Billing 8.0.0 — Google's Cloud CCT
         telemetry transport. Billing's own manifest declares only BILLING; the
         telemetry rides in on a transitive edge that no README mentions. Removing
         it is what makes decision-record §3.1's sentence true: the app itself
         cannot connect to anything. Only tiers 1 and 2 are claimable — the share
         sheet is another process, and a CSV a shepherd AirDrops does leave the
         phone.

         POST_NOTIFICATIONS is merged by flutter_local_notifications, a dependency
         since N00-T03. v1.0.0 creates no notification channel and never asks for
         the permission; reminders are v1.1.0. A permission declared for a feature
         that does not exist is a line in the Play listing a shepherd can read and
         a promise the app does not keep. N24 deletes this second line. -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:node="remove" />

    <application
        android:label="shed_book"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
