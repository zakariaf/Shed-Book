<?xml version="1.0" encoding="utf-8"?>
<!--
  THE NON-NIGHT FOLDER, AND IT MUST BE DARK ANYWAY.

  values-night/ is only consulted when the phone is in dark mode. A shepherd
  whose phone is in light mode still gets this file, and this app has no light
  theme — so a light launch background here would be a white flash on exactly the
  devices whose owners never turn dark mode on.
-->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/shed_surface_base</item>
        <item name="android:statusBarColor">@color/shed_surface_base</item>
        <item name="android:navigationBarColor">@color/shed_surface_base</item>
        <!-- Stops the OS "helpfully" inverting our surfaces on Android 10+. -->
        <item name="android:forceDarkAllowed">false</item>
        <!-- Both default TRUE, and both paint a translucent scrim behind the
             system bars that reads as a lighter band across the top and bottom
             of a page whose whole claim is that it is one field. -->
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/shed_surface_base</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
